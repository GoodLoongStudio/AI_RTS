# 待删除残留清单（2026-09-01，阶段 0.3）

> 本批共 61 个诊断产物，已删 43 个；以下 18 个二进制文件被 IDE 文件锁挡住（DeleteFile 报
> recordFileDelete=false），需关闭 IDE 后手工删除或重启后重试。全部为历史调试截图与部署
> 产物，无工程引用（debug_ai_probe 仅自引用；yyp_test.bundle 是联机 Demo 部署增量包，文档
> 引用的分支名不受影响）。删完后本文件一并删除。

```
diag_after_click.png
diag_build_click.png
diag_current.png
diag_physical.png
diag_restored.png
diag_worker_selected.png
dual_a_after_move.png
dual_a_minimap_fix.png
dual_b_after_move.png
dual_b_minimap_fix.png
runtime_no_fog.png
runtime_playable.png
runtime_single.png
runtime_single_after_bg_fix.png
runtime_single_fog_fix.png
runtime_single_latest.png
runtime_single_no_fog_after_bg_fix.png
test_diag.png
yyp_test.bundle
```

PowerShell 一键删除（在 g:\AIRTS\AI_RTS 下）：

```
Get-Content '待删除残留清单-阶段0.3-勿提交.md' | Where-Object { $_ -match '\.(png|bundle)$' } | ForEach-Object { Remove-Item $_ -ErrorAction SilentlyContinue }
```
