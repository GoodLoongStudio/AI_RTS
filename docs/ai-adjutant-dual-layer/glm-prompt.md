# 可直接交给 GLM5.3flash 的任务

请实施 AI_RTS 双层副官改造的第一阶段，完成后交回审查。你负责具体设计映射、编码、测试和报告，不需要用户逐项操作。第二阶段接真实双模型由审查后另行安排，本轮不要越过该交付节点。

工作区 G:\AIRTS\AI_RTS；副官脚本 G:\AIRTS\临时文件夹\hermes_skill。

先完整阅读项目内以下文件，并将其视为本任务规格：

1. docs/ai-adjutant-dual-layer/README.md
2. docs/ai-adjutant-dual-layer/architecture.md
3. docs/ai-adjutant-dual-layer/delivery.md

先检查适用工作区说明与实际代码。文档里的接口是拟议契约，必须映射真实运行时，不得假装现有字段已经存在。优先复用 IGameBalanceCatalog、资产 manifest、CommandRuntime、UnitCommandGateway 和现有回执链，避免第二套资源、兵种或物理系统。

本轮结果应包含：动态规则导出、足够的战术观测、带对局/玩家身份和版本的命令协议、幂等和过期处理、玩家优先权、可测试的协调器核心、持久化并发隔离，以及 delivery.md 第一阶段所列场景的测试证据。假模型只用于确定性协议测试，不能称作真实双模型闭环。

用户未来三天还会让其他工具大量新增或修改单位与建筑。请将数值、生产关系和能力绑定当前对局规则，稳定 ID 与显示名分离；未知机制显式 unsupported，不用硬编码坦克/工厂名单冒充通用支持。用独立测试配置验证至少一种新增普通内容和成本变更，不改用户正式平衡数据来凑测试。

请先记录工作区基线并尽量在独立 worktree 中工作；若已有脏修改与本任务相关，必须保留并说明如何继承，不能 reset/checkout/clean/stash。当前地图文件变化是其他任务内容，不得恢复或清除。不要 git commit/push，不 SSH，不部署，不修改线上 Hermes 配置，不接 DGX。

正常实现选择请自主处理。若契约和实际系统不一致，优先选择兼容现有架构且保持契约语义的方案，将偏差写进报告；不要以“需要确认”为由停在纯计划，也不要为适配而大范围重写无关系统。

先跑有区分力的确定性测试，再跑本地双进程的真实生产/建造和观测链。不要用无限重试或最好成绩隐藏失败，不要求为了凑次数重复同一无效测试。记录首次失败、重试、版本与独立日志路径。不能直接用旧报告的通过数作为本轮结果。

最终将 review-report.md 写入 docs/ai-adjutant-dual-layer/，包含：

- 实际修改文件、工作位置/分支、基线和规则版本。
- 协议映射、兼容行为、与设计的偏差及理由。
- 测试命令、PASS/FAIL/SKIP/FLAKY、原始证据路径和未解决问题。
- 哪些已通过真实引擎验证，哪些仅为假模型测试，哪些未验证。
- 第二阶段的明确接入点与剩余工作。

第一阶段完成后停止新增功能，等待审查。不要宣称双模型上线、DGX 接入或副官整体实战通过。
