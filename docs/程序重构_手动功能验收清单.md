# 程序重构：手动功能验收清单

## 2026-08-12 — Tank C# 纵向样例人工结果

- `TestOneUnit` 右键移动：通过；
- 连续移动命令替换：通过；
- 多单位移动：场景只有一个单位，本轮未验证；
- 攻击与停止后继续攻击：场景无敌方单位，本轮未验证；
- 单位碰撞：场景单位不足，本轮未验证；
- 传统 HUD 停止：2026-08-12 人工验收通过；普通 Selection 可直接对 Tank 强制移动和停止，不依赖“作战小队”，两项命令均返回接受且视觉表现正常；
- Legacy 问题隔离：AICommandHUD 的 `unit_group_1..3` 作战小队状态不再作为传统命令入口，Legacy HUD 继续默认隐藏。

后续验收需要新增至少两个可控单位、一个敌方单位和碰撞/导航障碍的专用场景，不能继续用 TestOneUnit 证明上述能力。

2026-08-14 已新增 `TestMultiUnitCommands.tscn`：包含两辆己方 Tank、一个己方不可移动 CommandCenter、一辆敌方 Tank 和地图障碍。自动化验证了混合能力/所有权批次的 `PartiallyAccepted`、逐单位错误、独立订单、重复 ID 去重与逐单位暂停；人工测试可用于观察两辆 Tank 同时移动，但导航拥堵和编队质量不属于本轮验收。

当前测试辅助调整：Legacy AICommandHUD 默认隐藏，可通过左上角按钮显示；Demo 强制禁用鼠标边缘滚屏，镜头平移只使用键盘。

2026-08-12 基础人工验收通过：传统 HUD 的单位状态切换已确认生效；警戒返回岗位、追击上限和混合多选显示仍按《程序重构_Tank战斗策略执行纵向样例记录》6 节继续观察。

2026-08-12 Tank 实体 ForceAttack 人工验收通过：`TestForceAttack` 的即时反馈及视觉表现符合预期；详细语义见《程序重构_Tank实体ForceAttack纵向样例记录》7 节。

2026-08-14 `CMD-016` 人工验收通过：`TestForceAttack` 可对地持续强制攻击；停止按钮可终止炮击；停火状态下显式强制攻击仍可用，停止后仍保持停火。

2026-08-14 新增 `CMD-017` 待人工验收：运行 `tests/manual/TestForceAttack.tscn`，分别测试移动、普通 Attack、实体/地面 ForceAttack 中点击“停止”。每次均应只反馈一次 Stop 接受；移动立即停止，当前攻击订单立即取消，侵略/警戒/固守及自由开火/停火选择不应改变。策略允许时单位可能立即自主重新索敌，看起来继续攻击，但旧普通 Attack 订单已经取消；切换停火后可隔离并确认该行为。

2026-08-14 `CMD-017` 首轮人工验收发现并已修复：远距离地面 ForceAttack 的接近射程阶段没有在 Action 退出时清理导航目标。请重点复验“Tank 明显开始前进后点击停止”，确认其立即停在当前位置，不再继续进入射程。

2026-08-14 `CMD-017` 人工复验通过：实体/地面 ForceAttack 均可正确停止，自主重新索敌正常。当前动画帧数较少，普通 Attack 被 Stop 取消的视觉不够明显，其订单状态和 Action 清理由自动测试覆盖。

2026-08-14 新增 `CMD-028` 验收原则：任何已经发射的炮弹、导弹或其他攻击都应独立完成视觉与伤害结算，即使发射单位随后 Stop、切换命令或阵亡；已经发射的投射物不得因发射者退出 SceneTree 而消失。目标先行失效时应继续完成合理的末端视觉，但不得对已失效引用重复结算伤害。

2026-08-14 `CMD-028` 代码和自动验证完成，待人工视觉验收：在 `TestForceAttack.tscn` 中发射炮弹后立即 Stop，已发射炮弹仍应飞完并在视觉抵达时造成伤害；HoldFire 下 ForceAttack 后 Stop，在途弹允许命中，但不得继续生成新炮弹且持续策略仍为 HoldFire；地面 ForceAttack 也应在炮弹抵达时而非发射时扣血。来源阵亡与 Rocket 目标先失效由 `ProjectileLifecycleSmokeTest.tscn` 自动覆盖，详细记录见《程序重构_投射物生命周期与伤害结算纵向样例记录》。

2026-08-14 `CMD-028` 人工验收通过：能够明显观察到 Stop 后在途炮弹仍完成一次伤害。当前弹体飞行较快，更慢弹速下的轨迹一致性、有限制导、脱靶和范围伤害将在具体武器方案确定后做更详细专项测试。

2026-08-14 新增 `CMD-026A` 待人工验收：运行 `tests/manual/TestHelicopterCommands.tscn`，选择蓝方 Helicopter。普通右键地面、强制移动、移动并攻击和撤退均应可执行；Helicopter 没有倒车能力，因此撤退退化为普通移动。传统 HUD 与右下角 Legacy `X` 均应能 Stop。HoldFire 应拒绝普通右键攻击，ForceAttack 仍可发射 Rocket 且停止后保持停火；对地 ForceAttack 应返回拒绝，因为当前 Helicopter 未声明该能力。

2026-08-14 `CMD-026A` 人工验收通过：Helicopter 的攻击、停火、撤退等运行正确。验收同时发现两个独立语义问题：ForceMove 状态不消费实体点击；空中单位普通右键地面资源时仍复用平面 footprint 相接距离。两项分别登记为 `CMD-029`、`CMD-030`，不计为本切片回归。

2026-08-14 新增 `CMD-029` 待人工验收：运行 `tests/manual/TestHelicopterCommands.tscn`，先将 Tank 与 Helicopter 切换为停火以隔离自主攻击。分别选择单个单位，点击“强制移动”后右键红方建筑。两次都应反馈 ForceMove 接受且不应转入普通攻击：Helicopter 应飞向实际点击位置上空；Tank 应尝试向点击位置移动，但可被建筑的地面导航 footprint/碰撞挡在外侧，此现象留给 `CMD-023`，不属于本项失败。

2026-08-14 `CMD-029` 人工验收通过：强制移动可把敌方建筑作为位置目标，Helicopter 正常停在建筑上方。Tank 会贴紧建筑来回踱步，已并入导航专项记录，暂不在当前重构分支修复。

2026-08-14 新增 `CMD-030` 待人工验收：运行 `tests/manual/TestHelicopterCommands.tscn`，单选 Helicopter，普通右键一个距离较远的矿石模型。Helicopter 应飞到实际点击位置上空，而不是按双方平面 footprint 停在矿石旁；随后普通右键红方建筑仍应执行 Attack。单选 Tank 普通右键矿石时仍应停在其 footprint 外侧，不得套用空中规则。

2026-08-14 `CMD-030` 人工验收通过：Helicopter 普通右键矿石会按预期移动到点击位置上空；既有敌方实体 Attack 与地面 Tank footprint 规则未发现回归。

2026-08-14 新增 `CMD-026B` 待人工验收：运行 `tests/manual/TestAllUnits.tscn`，选择矿石旁的 Worker，普通右键附近存量为 2 的 `ResourceB5`。开始采集时应收到 Gather 接受；Worker 尚未返回 CommandCenter 前，左上角玩家 B 资源仍为 0。采到第一份后点击传统 HUD“停止”，Worker 应完全暂停且不会自行恢复。再次右键同一矿石会创建新任务；Worker 采完两份、返回 CommandCenter 后玩家 B 资源一次增加到 2。该矿耗尽后 Worker 应待机，不得自动转向附近其他矿点。返程死亡丢失载荷与 `UnitLost` 由自动测试覆盖。

2026-08-14 `CMD-026B / ECO-002` 人工验收通过：Worker 的采集—返程—交付循环运行正常。验收同时记录 `CMD-031`：Worker 携带载荷被 Stop 暂停后，当前必须再次右键资源点才能建立新 Gather 并先返程交付；右键 CommandCenter/资源交付建筑尚不会触发交付。该项等待策划组统一评审上下文右键行为，本轮不修改代码。

2026-08-14 新增 `ECO-001` 待人工验收：运行 `tests/manual/TestAllUnits.tscn`。初始玩家资源不得为负；Worker 交付后左上角资源条应与此前一样增长。余额不足时尝试生产或放置建筑，操作应被拒绝且 A/B 均不得被部分扣除或变成负数；采集到足够资源后，生产入队应一次扣除完整 A/B 成本，取消生产应按当前规则全额退还。C# 权威快照、Legacy 字段、事件原因、重复交易与施工退款由自动测试覆盖。

2026-08-14 `ECO-001` 部分人工验收通过：资源不足时无法建造，符合当前“完整成本均足够后一次性原子扣款”规则。Worker 交付已有前序人工结果；生产入队扣款与取消退款仍待人工检查。验收同时发现 `ECO-010` Legacy 缺陷：新建筑开始施工时会误播“建筑正在被攻击”，不计为权威资源账户回归，本轮只记录原因、不修改代码。

2026-08-14 `ECO-001` 综合验收完成：用户已确认资源不足时无法建造；Worker 交付已有人工验收，生产扣款/取消退款、施工退款、账户镜像和事件原因已有 Godot 自动覆盖，重复交易与原子余额规则已有纯 C# 覆盖。本项结项，尚未单独人工观察的生产 UI 动画继续保留在完整回归清单中。

## ECO-003 建筑放置合法性人工验收

建议使用正常 Demo 或 `TestAllUnits.tscn`，选择 Worker 打开建筑蓝图：

1. 在己方视野内空地放置，蓝图应显示合法并成功扣款、生成施工现场；
2. 把完整 footprint 的一部分移出己方视野，蓝图应提示 `Insufficient vision` 且不能放置；
3. 把建筑中心放在地图内、但边缘伸出地图，仍应拒绝；
4. 覆盖己方 Tank/Worker 放置时蓝图应允许；建筑生成后重叠友军应走到 footprint 外，旧任务结束，完成驱逐后待命；
5. 覆盖敌方地面移动单位、任意建筑、施工现场或资源节点时应拒绝；
6. 空中单位位于地面蓝图上方时不应阻挡；
7. 余额不足时仍应拒绝且不得部分扣款；
8. 连续预览不得扣款；预览后若空间或余额变化，最终点击应按最新状态拒绝；
9. 规则 AI 建造 CommandCenter、防御塔或生产建筑时应继续正常工作，不再自行绕过统一复验。

当前自动证据：纯 C# 共 31 项 0 failure；`StructurePlacementSmokeTest` 与 `EconomyAccountSmokeTest` 均为 0 failure；`TestPlayerVsAI` 运行 600 帧未发现新的脚本错误。Godot 退出时的 RID/ObjectDB 泄漏为已有基线问题。

2026-08-15 `ECO-003` 人工验收通过：用户确认视野内空地正常放置、覆盖友军后能自动散开让出位置、视野外无法放置，均符合预期。本项结合自动证据结项；敌方占位、空中单位、多友军拥挤和最终竞态继续保留在完整回归清单，不阻塞当前权威放置边界完成。

## ECO-004 施工生命周期人工验收

建议运行正常 Demo 或 `tests/manual/TestAllUnits.tscn`，选择 Worker 放置一座建筑：

1. 进入蓝图模式时已经选中的 Worker 应在 Place 成功后前往现场施工；蓝图模式中途改变选择不应偷偷更换建造者；
2. Worker 到达前或施工中点击传统 HUD“停止”，应停止移动/火花/进度，且不会自动恢复；
3. 再次右键同一未完成建筑，应继续原施工任务；
4. 多选 Worker 右键同一现场时均可施工，完工应明显快于单 Worker；
5. 一个 Worker 中途收到 Move/Gather 等其他命令后，其余 Worker 仍可完成建筑；完工后退出者和完工者都不应残留施工动作；
6. 选中未完成建筑主动取消，应删除现场并全额退款；敌方摧毁未完成建筑不应退款；
7. 施工期间建筑受伤后应带伤完工，不得自动回满；
8. 新放置建筑不应再播放“建筑正在被攻击”；施工中受到真实伤害时仍应保留受击反馈；
9. 建造完成事件、声音和可用状态只应出现一次；
10. 规则 AI 仍能建造，并保持首轮一次只分配一个 Worker 的策略。

当前自动证据：35 项纯 C# 测试 0 failure；`StructurePlacementSmokeTest` 覆盖 Construct、Stop、同订单恢复、完工清理和损伤保留，0 failure；`RuntimePlacementSmokeTest` 0 failure。Godot 退出时的既有 RID/ObjectDB 泄漏继续按导航/测试运行器缺陷基线处理。

2026-08-15 `ECO-004 / ECO-010` 人工验收通过：用户确认施工带伤保留、主动取消与被摧毁的退款结果不同、进入蓝图模式时捕获建造者等功能正常。本项结合自动证据正式结项。

## ECO-005 生产队列生命周期人工验收

运行 `tests/manual/TestAllUnits.tscn`，给 Human 补充足够 A/B 资源后检查：

1. CommandCenter 只能生产 Worker，VehicleFactory 只能生产 Tank，AircraftFactory 只能生产 Helicopter/Drone；
2. 同一建筑连续加入两项时，只有队首推进；首项出厂后第二项自动开始；
3. 每个生产建筑最多保留 5 个未完成项目，第六项不得扣款或入队；
4. 取消排队中或生产中的项目后队列项消失，并按首轮规则全额退回已经支付的 A/B 资源；
5. 生产完成后单位只生成一次，HUD 项目移除，既有完成语音不重复；
6. Worker、Tank、Helicopter 和 Drone 均按各自移动领域在对应生产建筑附近出厂；
7. 若方便构造出口拥挤，完成项目应等待合法部署位置，不得重叠生成，也不得越过它推进后续项目；
8. 生产建筑被摧毁后，所有未完成项目终止且不退款；
9. 规则 AI 仍可正常生产，但不再通过 `ignore_limit` 绕过统一容量；
10. 当前不存在生产暂停和调整顺序功能，本轮不将其列为缺失或回归。

2026-08-15 `ECO-005` 人工验收通过：用户确认队列表现、取消退款和不同建筑的队列隔离均正常；每个单位各播放一次 `training` 与一次 `unit ready`，没有重复开始或完成语音。结合纯 C# 自动证据与成功构建结果，本项正式结项。

## ECO-006 集结点与出厂默认策略人工验收

运行 `tests/manual/TestAllUnits.tscn`：

1. 选中 CommandCenter，右键地图内地面设置集结点；绿色标记应移动，新生产 Worker 出厂后向该位置移动；
2. 分别为 CommandCenter 与 VehicleFactory 设置不同位置，之后再修改其中一个；两座建筑状态和出厂单位目的地不得互相覆盖；
3. 选中 CommandCenter，右键己方移动单位作为目标；新 Worker 应持续跟随该目标；目标被移除后绿色标记回到建筑门口，后续 Worker 留在默认出口；
4. 以资源节点作为 CommandCenter 集结目标，新 Worker 出厂后应直接开始 Gather；Tank、Helicopter 等非 Worker 以资源为目标时只靠近，不得获得采集能力；
5. 选中生产建筑后点击传统命令栏“清除集结”，绿色标记回到建筑门口；之后出厂单位不再执行旧目标；
6. 尝试右键 Producer 自身或敌方实体，不得建立实体集结目标；
7. 给 CommandCenter 设置“固守”和“停火”，再生产 Worker；Worker 出厂后应显示继承 `HoldGround / HoldFire`，随后再执行 Rally；
8. 修改 Producer 策略只影响之后出厂的单位，已经出厂的单位不得被追溯修改；
9. 生产完成、策略继承或 Rally 移动失败都不得退款，也不得把生产项目退回队列；
10. 选中无武器生产建筑时，姿态、停火和清除集结按钮可用；强制移动、攻击、撤退仍按其当前能力禁用或拒绝。

建议也运行 `tests/automated/RallyPointSmokeTest.tscn`。自动场景覆盖独立状态、位置分派、策略继承、Clear 和资源目标失效；当前命令行 headless 在两个既有 Godot 编辑器进程同时打开时等待超时，尚未取得运行结论。

2026-08-15 `ECO-006A / ECO-006B` 人工验收通过：用户确认设置、清除和覆盖集结点均生效；生产建筑模式设置生效，之后生产的单位正确继承模式。集结点与出厂默认策略正式结项。

## ECO-012 连续生产旁白人工复验

在同一生产建筑中连续排入至少三个单位：

1. 队列从空变为活动时只播放一次 `training`；第二、第三项取得生产线时不再重复播放；
2. 每个单位完成时应优先听到一次 `unit ready`，不得被下一项同帧开始事件覆盖；
3. 队列完全清空后重新入队，应开始新的连续生产链并再次播放一次 `training`；
4. 连续项目全部取消后再次入队，也应重新播放 `training`；
5. 完成提示播放期间，低优先级 `training` 或普通资源提示不得覆盖它；胜利/失败等终局提示仍拥有最高优先级。

实现按 Producer 分别追踪连续生产链；不同建筑各自从空队列开始时均可产生一次 `training`。

2026-08-15 `ECO-012` 人工验收通过：当前连续生产语音符合预期，生产旁白调度工作项结项。

2026-08-12 Tank 战术撤退人工验收通过：撤退方向正确，后退过程中会自动攻击。当前地图没有障碍，路径转弯尚未人工验证；停火表现和复杂射界边界仍保留为后续专项验收。

2026-08-13 Tank 普通移动伴随射击人工验收通过：`TestMoveFire` 中移动过程自动攻击正常，其他功能正常。该场景不测试 AttackMove。

2026-08-13 新增待人工验收：运行 `tests/manual/TestGroundAttackMove.tscn`，选择 Tank 后点击“移动并攻击”并右键地面。停火时应只推进；固守只处理射程内敌人且交战时不离开接敌点；警戒可有限离开路径但不持续追击；侵略可在更大感知范围接敌并追近；清敌后均应恢复原推进目的地。

2026-08-13 Tank 普通实体 Attack 人工验收通过：`TestOrdinaryAttack` 中普通右键敌方单位的执行与反馈符合预期。

2026-08-13 新增待人工验收：运行 `tests/manual/TestEntityAttackMove.tscn`，选择 Tank 后点击“移动并攻击”并右键敌方单位。单位应保留该最终目标，途中接敌结束后继续追踪；停火时只追踪不射击；最终目标死亡后停止追踪且订单进入 `TargetLost`。

2026-08-13 修复待人工复验：运行时单位与建筑不应再堆叠到左上角原点。`TestCombatPolicies` 中 Tank 应保持约 `(13.263, 0, 12.637)`，敌方建筑应保持约 `(20, 0, 12.637)`；随后复验地面/实体 AttackMove 与普通 Attack 的真实距离表现。

2026-08-13 新增待人工验收：运行 `tests/manual/TestOrdinaryAttack.tscn`。自由开火时直接右键红方目标应建立普通 Attack 并造成伤害；停火后直接右键红方目标应返回拒绝且不执行；强制攻击仍可临时覆盖停火；普通右键己方目标不得被解释为攻击。

> 用途：记录重构前实际行为，并在每个迁移切片后回归比较。
>
> 基线提交：`15ffbf3`
>
> 推荐引擎：Godot 4.7 Mono
>
> 状态标记：`[ ] 未测`、`[x] 通过`、`[!] 异常`、`[-] 不适用`

## 1. 使用方法

1. 在 Godot Project Manager 中导入 `AI_RTS-refactor/project.godot`，不要误开另一个工作树。
2. 等待首次资源导入结束；`.godot/` 是本地缓存，不提交。
3. 首轮按“主项目流程”体验，再对 `tests/manual` 中五个场景逐个使用“运行当前场景（F6）”。
4. 每项填写状态、实际表现、截图/录像路径、测试提交和测试人。
5. 崩溃、卡死、文本异常、行为不同但仍可玩，都不能直接写“通过”，应写明差异。

建议记录格式：

```text
验收项：RTS-SEL-01
状态：[x]
提交：15ffbf3
环境：Godot 4.7 Mono / Windows
实际结果：左键单选、拖框多选正常
证据：docs/evidence/...
备注：无
```

## 2. 已知基线注意事项

- 现有 AICommandHUD 是关键词 Mock，不会连接大模型。
- 自动化 `CampaignSmokeTest` 当前存在假阳性：设置按钮节点名断言失败后仍打印成功并退出 0。
- 首次运行需完成模型和翻译等资源导入；不要提交 `.godot/imported`。
- 战斗中攻击者与目标水平位置重合会在通用攻击朝向逻辑持续触发 `looking_at` 错误，可能表现为直升机或整局卡住；重构后出现同一现象不能直接记为迁移回归。
- 初始建筑靠近地图角落、部分模型越界、边缘滚屏范围过大，以及建造/生产交互时仍触发边缘滚屏，均已作为旧版本问题登记。
- 详细缺陷归因和素材来源见《程序重构_素材来源审计与既有问题登记.md》。

## 3. 主项目流程

### 3.1 启动与菜单

- [ ] `BOOT-01` 运行 `source/Main.tscn` 后显示 Logo，并自动进入主菜单。
- [ ] `MENU-01` 主菜单存在单人战役、自定义对局、设置、制作人员和退出入口。
- [ ] `MENU-02` 制作人员页面可进入并返回。
- [ ] `MENU-03` 设置页面可进入并返回，窗口模式和鼠标限制可保存。
- [ ] `MENU-04` 设置页可调整边缘滚屏、移动速度、边缘范围、底边范围、平滑度和缩放步长，并可恢复默认。

### 3.2 自定义对局

- [ ] `CUSTOM-01` 进入自定义对局后可看到 `Plain & Simple` 与 `Big Arena`。
- [ ] `CUSTOM-02` 地图分别限制为最多 4 人和 8 人槽位。
- [ ] `CUSTOM-03` 至少配置两个玩家后可开始；同一局最多允许一个 Human。
- [ ] `CUSTOM-04` Human 与 Simple Clairvoyant AI 可组合，颜色和出生偏移生效。
- [ ] `CUSTOM-05` 点击开始进入 Loading，随后进入 Match；失败时有错误提示而不是永久卡住。
- [ ] `CUSTOM-06` 自定义对局页面的设置入口可以打开统一设置面板。

### 3.3 镜头与小地图

- [ ] `CAM-01` W/A/S/D 平移，Q/E 旋转，滚轮缩放。
- [ ] `CAM-02` 鼠标靠近屏幕边缘可滚屏，底边触发区比其他边更宽。
- [ ] `CAM-03` 镜头不会越出地图计算边界，非方形地图也正常。
- [ ] `CAM-04` 小地图显示单位标记和相机视野框。
- [ ] `CAM-05` 小地图左键点击/拖动移动镜头，右键为已选单位下达移动命令。
- [ ] `CAM-06` 暂停菜单内修改镜头参数能立即作用于当前镜头。

### 3.4 选择与编组

- [ ] `RTS-SEL-01` 左键可单选己方单位，点击其他己方单位切换选择。
- [ ] `RTS-SEL-02` 左键拖框可多选己方移动单位。
- [ ] `RTS-SEL-03` 按住 Shift 点击已选单位可从选择中移除。
- [ ] `RTS-SEL-04` 双击一种单位可选择当前可控的同类单位。
- [ ] `RTS-GRP-01` `Ctrl+1..9` 保存编组，`1..9` 召回编组。

### 3.5 移动与目标交互

- [ ] `RTS-MOVE-01` 选择地面单位后右键地面，单位沿地面导航移动。
- [ ] `RTS-MOVE-02` 选择空中单位后右键地面，单位使用空中导航移动。
- [ ] `RTS-MOVE-03` 多单位移动时不会长期互相卡死；卡住处理可恢复大部分单位。
- [ ] `RTS-TGT-01` 右键敌方单位触发追踪/攻击，而非普通移动。
- [ ] `RTS-TGT-02` 右键资源单位时，Worker 开始采集；非 Worker 不执行采集。

### 3.6 资源、建造与生产

- [ ] `ECO-01` Worker 可采集 Resource A、Resource B，达到容量或完成采集后送回建筑。
- [ ] `ECO-02` 玩家资源条随采集、建造和生产实时变化。
- [ ] `BUILD-01` 选择 Worker 后显示 CommandCenter、VehicleFactory、AircraftFactory、AntiGroundTurret、AntiAirTurret 建造按钮。
- [ ] `BUILD-02` 蓝图跟随鼠标；越界、碰撞或资源不足时不能放置并显示反馈。
- [ ] `BUILD-03` R 可按 45° 步长旋转建筑蓝图，右键可取消放置。
- [ ] `BUILD-04` 放置后 Worker 前往建筑并施工，完成后建筑进入正常状态。
- [ ] `PROD-01` CommandCenter 可生产 Worker；VehicleFactory 可生产 Tank；AircraftFactory 可生产 Helicopter/Drone。
- [ ] `PROD-02` 生产队列显示进度并支持取消，取消后的资源规则与当前实现一致。
- [ ] `RALLY-01` 选中生产建筑后右键地面可设置集结点，出厂单位自动前往。
- [ ] `RALLY-02` 右键一个合法单位可将其设为动态集结目标，目标删除后不崩溃。

### 3.7 战斗、可见性与胜负

- [ ] `COMBAT-01` Tank/对地炮塔攻击地面目标，Helicopter/对空炮塔的目标域限制正确。
- [ ] `COMBAT-02` 单位在待命/警戒状态会自动寻找射程或视野内目标。
- [ ] `COMBAT-03` 伤害降低 HP，HP 为零后单位死亡、清理选择和小地图标记。
- [ ] `FOW-01` 按玩家可见性模式下，未探索区域被战争迷雾遮挡。
- [x] `FOW-02` 离开视野的移动敌军隐藏；已见过的敌方建筑残影符合当前实现。2026-08-15 人工验收通过，明暗程度后续由表现/UI 专项调整。
- [ ] `END-01` Human 单位全部被消灭显示失败；对手全部被消灭显示胜利。
- [ ] `END-02` 结束页和暂停菜单均可安全退出本局。

## 4. 单人战役：回声撤离

从主菜单进入“单人战役”，选择当前唯一可用任务。

- [ ] `CAMP-01` 战役列表显示任务 01，后续任务为未解锁；设置和返回入口可用。
- [ ] `CAMP-02` 进入任务后加载 600×450 `EchoExtractionGreybox`。
- [ ] `CAMP-03` 玩家只有一个可控的先锋英雄；不存在第二、第三玩家小队；敌方单位已生成。
- [ ] `CAMP-04` HUD 显示当前目标、任务叙述、AI 副官区和英雄卡片。
- [ ] `CAMP-05` F1 选择英雄；350 ms 内双击 F1 锁定或解除镜头跟随。
- [ ] `CAMP-06` 依次到达 SignalGate、PerimeterCamp、AbandonedConvoy 后目标推进。
- [ ] `CAMP-07` 在黑箱读取阶段对英雄下达 DEFEND 或 STOP，等待约 2 秒后目标推进为返回撤离点。
- [ ] `CAMP-08` 到达 EmergencyExtraction 后出现“请求撤离”，点击显示任务结果页。
- [ ] `CAMP-09` 结果页可返回单人战役列表。
- [ ] `CAMP-10` 中文任务文本和 AI HUD 文本可读，标点、换行与字体回退正常。

### 4.1 AI 指令原型

- [ ] `AICMD-01` Q/W/E/R/D/F 分别进入移动、攻击、警戒、侦察、撤退、停止意图。
- [ ] `AICMD-02` 移动/侦察/撤退需再右键地图确认；攻击需再右键已确认敌军。
- [ ] `AICMD-03` 输入“去前面看看”“原地警戒”“攻击目标”等关键词可映射命令。
- [ ] `AICMD-04` 输入“下一步做什么”“风险”“当前状态”等问题返回当前任务预设信息。
- [ ] `AICMD-05` 无法解析的文本不会擅自执行命令，而是提示可用表达。
- [ ] `AICMD-06` 断网状态下上述功能仍工作，从而确认它是本地 Mock；不得记录为真实 LLM 验收通过。
- [ ] `AICMD-07` 冻结期版本提供 AI 副官界面显示/隐藏入口，并记住本地设置。
- [ ] `AICMD-08` AI 副官界面隐藏后不遮挡地图、不拦截鼠标，也不响应 LegacyAgent 快捷键。
- [ ] `INPUT-01` 集中按键配置可以加载默认值与 `user://controls.cfg` 用户覆盖。
- [ ] `INPUT-02` 镜头、单位命令、建筑放置、文本输入和 LegacyAgent 的按键冲突可检测，并按上下文正确启停。

## 5. 手动测试场景

在 Godot 文件系统面板双击场景，然后按 F6。

### 5.1 `tests/manual/TestOneUnit.tscn`

目的：最小化检查 Human、单个 Tank、选择、移动、镜头、战争迷雾、小地图。

- [ ] `SCENE-ONE-01` 场景可独立启动，无缺失依赖。
- [ ] `SCENE-ONE-02` Tank 可选择、移动、显示选择圈，并反映在小地图上。

### 5.2 `tests/manual/TestAllUnits.tscn`

目的：一次查看九种单位/建筑、资源点和各上下文菜单。

- [ ] `SCENE-ALL-01` CommandCenter、AircraftFactory、VehicleFactory、两类炮塔、Drone、Helicopter、Tank、Worker 均出现。
- [ ] `SCENE-ALL-02` 各单位选择后 HUD 菜单正确切换，生产和建造入口可操作。
- [ ] `SCENE-ALL-03` 两种资源点可被 Worker 采集。

### 5.3 `tests/manual/TestUnitsFightingEachOther.tscn`

目的：集中观察目标域、自动索敌、投射物、伤亡和清理。

- [ ] `SCENE-FIGHT-01` Human 多个对空炮塔与 AI 多个 Helicopter/Drone 能按目标域交战。
- [ ] `SCENE-FIGHT-02` 长时间运行不出现已死亡单位残留、持续攻击无效引用或明显错误刷屏。

### 5.4 `tests/manual/TestPlayerVsAI.tscn`

目的：观察完整 Human 对 SimpleClairvoyantAI 的经济、建造、生产和进攻循环。

- [ ] `SCENE-AI-01` AI 会补充 Worker/CommandCenter，并自动采集两种资源。
- [ ] `SCENE-AI-02` AI 会请求资源并建造对地/对空炮塔与进攻工厂。
- [ ] `SCENE-AI-03` AI 会生产 Tank/Helicopter，形成编队并攻击对手。
- [ ] `SCENE-AI-04` Drone 会在单位目标之间移动，体现全知侦察控制器行为。

### 5.5 `tests/manual/TestNonQuadraticMap.tscn`

目的：检查系统没有把地图错误假定为正方形。

- [ ] `SCENE-RECT-01` 地图、导航、战争迷雾和小地图按非方形比例显示。
- [ ] `SCENE-RECT-02` 镜头边界、点击换算和小地图命令在长短边均正确。

### 5.6 `ECO-007B` 强类型配置运行时迁移

目的：确认资源、生产和施工改读同一 Match Catalog 后，玩家可见行为保持基线。

- [x] `CONFIG-B-01` Worker 对 Resource A/B 的采集、返程和交付循环正常，节奏没有明显变化。
- [x] `CONFIG-B-02` 三类生产建筑只能生产获准产品，成本、工期和每建筑独立队列正常。
- [x] `CONFIG-B-03` 资源不足不能入队；主动取消未完成产品仍全额退款。
- [x] `CONFIG-B-04` 五种建筑的预览、视野限制、友军驱逐、扣款、放置和施工均正常。
- [x] `CONFIG-B-05` 主动取消施工退款，被摧毁不退款，施工完成事件只发生一次。
- [x] `CONFIG-B-06` Godot 输出中没有 Balance Catalog、asset manifest 或未知定义错误。

### 5.7 `ECO-007C` 单位与当前主武器迁移

目的：确认基础属性和发射快照改读 Catalog 后，战斗、移动和视野行为保持基线。

- [x] `CONFIG-C-01` 九种单位/建筑的初始 HP、视野与战争迷雾表现正常。
- [x] `CONFIG-C-02` Tank、Worker、Drone、Helicopter 的移动速度与空地移动域正常。
- [x] `CONFIG-C-03` Tank 仍可倒车撤退、移动射击和对地强制攻击。
- [x] `CONFIG-C-04` Tank、Helicopter、两类炮塔的伤害、冷却、射程和目标域正常。
- [x] `CONFIG-C-05` Worker 载荷仍为两点，单 Worker 施工速度与迁移前一致。
- [x] `CONFIG-C-06` 停火、Stop 或发射者死亡后，已经发射的投射物仍完成一次命中伤害。
- [x] `CONFIG-C-07` Godot 输出中没有未知 UnitType、Weapon、Warhead 或 Projectile 映射错误。

### 5.8 `ECO-007D` HUD 与规则 AI Legacy 清理

目的：确认显示层与传统规则 AI 改读 Catalog 后，删除旧字典没有造成行为回归。

- [x] `CONFIG-D-01` Worker 和三类生产建筑菜单的所有 Tooltip 均正常显示。
- [x] `CONFIG-D-02` Tooltip 中 HP、伤害/间隔和 A/B 成本与迁移前一致。
- [x] `CONFIG-D-03` 四种产品的扣款、资源不足拒绝和取消退款正常。
- [x] `CONFIG-D-04` 五种建筑的扣款、放置、施工和取消退款正常。
- [x] `CONFIG-D-05` 传统规则 AI 能采集、请求资源、建造、生产并发起进攻。
- [x] `CONFIG-D-06` Godot 输出中没有 Catalog、manifest 或未知定义错误。

## 6. 自动化冒烟测试复核

当前可使用以下命令运行，但必须检查完整输出，不能只看退出码：

```powershell
& 'C:\Program Local\Godot_v4.7-stable_mono_win64\Godot_v4.7-stable_mono_win64_console.exe' `
  --headless `
  --path 'D:\Documents\Code\RTS_AI\AI_RTS-refactor' `
  'res://tests/automated/CampaignSmokeTest.tscn'
```

- [x] `AUTO-01` 2026-08-12：动态战役设置按钮已使用稳定节点名，正常冒烟测试通过并返回 0。
- [x] `AUTO-02` 2026-08-12：测试改用失败收集器；强制失败探针输出 `CAMPAIGN_SMOKE_TEST_FAILED` 并返回 1，不再假阳性。
- [!] `AUTO-03` 2026-08-11：退出时报告 2 个 NavObstacle3D、2 个 NavAgent3D、1 个 NavMap3D RID 泄漏。

在修复测试运行器之前，CI 判定规则至少应同时检查：没有 `SCRIPT ERROR`、没有 `Assertion failed`、存在成功标记、进程退出码为 0。长期应改成测试失败必定令进程返回非零。

## 7. 重构后差异记录

每个重构 PR 在此追加一行或链接独立报告：

| PR/提交 | 迁移切片 | 保持的行为 | 有意改变 | 未解决回归 | 验收人 |
|---|---|---|---|---|---|
| — | 重构前基线 | — | — | 假阳性、RID 泄漏 | 待填写 |
## 2026-08-15 — ARCH-014 自定义对局胜负判定验收

### 自动冒烟场景

运行：`res://tests/automated/MatchOutcomeRuntimeSmokeTest.tscn`

预期输出：

```text
Match outcome runtime smoke test completed: 0 failure(s)
```

该场景自动覆盖：

- 新生成敌方单位会继续维持对局，最后一个敌方单位死亡后 Human 胜利；
- Human 最后一个计分实体死亡后显示失败；
- 同一帧双方全灭得到 `Draw` 并显示 `Finish`；
- 无 Human 的 AI 对局保留结构化胜方并显示 `Finish`；
- 终局面板会暂停 SceneTree，测试会在各案例间自动恢复。

2026-08-15 首次运行发现 `MatchResolved` C# Signal 被错误地按 `match_resolved` 访问；修正为显式 PascalCase 连接后，已通过 Godot 编辑器复验，输出 `Match outcome runtime smoke test completed: 0 failure(s)`。

2026-08-15 `TestPlayerVsAI` 首轮实际对局未在清空敌军后显示 Victory。已修复仅监听 `unit_died` 导致取消蓝图等直接退出实体残留的问题；自动冒烟现在还会实例化真实 `TestPlayerVsAI`，调用实际 `Unit.gd` 死亡入口清空敌军，并检查 C# 结果与 Victory 面板。请先重新运行自动冒烟，再复验实际对局。

### 主对局人工回归

运行：`res://tests/manual/TestPlayerVsAI.tscn`

重点确认：

1. 开局不会提前出现结束面板或暂停；
2. 消灭敌方全部移动单位、建筑及未完工建筑后显示 `Victory`；
3. 己方全部移动单位、建筑及未完工建筑被消灭后显示 `Defeat`；
4. 胜负面板出现后对局暂停，胜利/失败语音仍各触发一次；
5. Exit 能解除暂停并返回主菜单。
