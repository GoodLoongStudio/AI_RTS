# 新执行方接手提示词（可发给任何无上下文的模型，如 Qwen3.8 Max）

---

你接手 `G:\AIRTS\RTS_Map_Tool`，这是一个为 Godot RTS（`G:\AIRTS\AI_RTS`）生成 4 人科幻废土地图的 Python 工具。前一位执行方已经按方案把 G1–G4 四个闸门跑通了一轮，代码能用，但 **G2 的地图布局算法方向错了**（生成的是均匀碎块噪声），现在要按修订版重做 G2。你的任务只到 G2 重做完成并汇报为止。

## 第一步：按顺序读完这三份文件，再动手

1. `docs/plan/4p-seeded-mapgen-plan.md` — 方案总纲（用户审核通过，唯一有效）。重点看 Implementation Strategy §3（唯一权威数据）、§6（画布与坐标）、Progress Tracking（当前状态）。
2. `docs/plan/执行提示词.md` — 执行细则。**【通用部分】的 10 条硬约束、review 目录规则、汇报格式必须全部遵守**；【G2】一节里"第一步：区域与通道"的折线与 `polar_order` 定义仍有效，"第二步：阻碍布局"已作废。
3. `docs/plan/执行提示词-修订1.md` — **本次要做的事**。§2 是新的雕刻式 G2 规格（区域参数、变宽度通道、雕刻流程、验收指标、overview 图定义、测试），§1 是产物精简规则，§4 是执行顺序。

三份文件冲突时：修订 1 > 执行提示词 > 方案总纲（后者只是被前者局部覆盖，不是失效）。

## 第二步：了解现有代码，不要重写整个仓库

- `rtsmap/` 是现有实现：`contract.py`（常量）、`rng.py`（闸门子 Seed）、`grid.py`（MapGrid/npz/哈希）、`gate.py`（approve）、`gates/g1_starts.py … g4_export.py`、`viz/canvas.py`（2048 画布，世界↔像素已实现）、`pathing.py`（膨胀/BFS/Dijkstra/通道宽度）、`report.py`；入口 `run.py`。
- `runs/<seed>/G1/` 是 64 个 Seed 的出生点结果，**保留不动**；`runs/*/G2|G3|G4` 是被否决的旧结果。
- 先跑 `python -m pytest tests -q` 确认现状全绿，再改。
- 只替换 `gates/g2_layout.py` 的算法部分与相关测试，加 `review/` 机制与 `--detail` 开关；`canvas.py`、`pathing.py`、`grid.py` 可以加函数，不要改已有函数的行为（G1 的哈希不能变）。
- `_legacy/` 是更早的失败实现，不得参考。

## 第三步：执行修订 1 §4

1. 把 `runs/*/G2`、`G3`、`G4`、`runs/G2_summary`、`runs/G3_summary` 移到 `runs_v1/` 同结构下保留。
2. 执行 `python run.py --approve G1 --seeds 8,24,45 --note "用户 2026-09-03 指定"`（用户已授权这一条）。
3. 实现 `review/<gate>/`（`contact.png`、`summary.md`、`seed_<N>_overview.png`）与 `--detail` 开关；补 `review/G1/`。
4. 按修订 1 §2 重写 G2，`algo_version` 升 2.0.0，只跑 `python run.py --gate G2 --seeds approved`（即 8、24、45）。
5. 跑测试。

## 第四步：停下汇报（不得进入 G3）

汇报 ≤ 40 行，内容：
1. 贴 `review/G2/contact.png` 与 `seed_8/24/45_overview.png` 共 4 张图
2. `review/G2/summary.md` 的表
3. 与修订 1 的任何偏差及原因（无则写"无"）
4. 需要用户决定的问题（无则写"无"）
5. 测试结果与复跑命令
6. 更新方案总纲 Progress Tracking：G2 标"✓ 雕刻式 v2 待确认"

## 红线（重复一次最容易踩的）

- 不做台地/高度/水；Y=0 平面 + 阻碍物。
- 代码不得回读 PNG；`mapgrid.npz` 是唯一权威；下游 npz = 上游通道原样复制 + 本闸门新增通道。
- 只用 numpy + Pillow + 标准库，禁 matplotlib；禁 Python `random` 与 `hash()`；同 Seed 重跑字节级一致。
- 画布 2048²、地图区 1920² 在 (64,64)、20 px/m、图像上方 = 世界 −Z；文字只能在边框区（通道最窄宽度数字例外，≤ 24 px）。
- 不改 `G:\AIRTS\AI_RTS`，不改 `G:\AIRTS\AI_RTS\初选素材包`。
- 汇报前自己打开 4 张 review 图看一遍：实体块是否成片、通道是否可见、咽喉数字是否合理；肉眼可见的问题先修再报。
- `approved` 只能由用户指令写入；做完 G2 立刻停。
