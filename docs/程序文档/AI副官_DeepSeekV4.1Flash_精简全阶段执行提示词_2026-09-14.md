# AI 副官：DeepSeek V4.1 Flash 精简全阶段执行提示词

你现在负责继续完成 `G:\AIRTS\AI_RTS` 的 AI 副官升级。必须连续执行“读取现状 → 修改实现 → 测试 → 分析失败 → 修复 → 复测”，不要停在分析或计划阶段，也不要做完一项后等待用户说“继续”。

## 一、先读取并遵守

按顺序读取：

1. `G:\AIRTS\AI_RTS\skill.md`
2. `docs\程序文档\AI副官_当前执行入口.md`
3. `docs\程序文档\AI副官_实战能力升级执行进度_2026-09-14.md`
4. `docs\程序文档\AI副官_高频扫描、多线并行与安全行军计划_2026-09-12.md`
5. 最新交接、运行日志、相关代码和测试

当前入口和最新进度优先于旧文档。历史文档只能作为背景，不能恢复旧的双模型、模型门控、等待用户继续或几何盲目前压方案。

## 二、今天必须完成

### 1. 真实模型闭环（不可延期）

完成并证明：

```text
真实游戏状态 → DeepSeek V4.1 Flash → 合法策略 → 副官协调层 → 规则执行器 → 游戏动作回执
```

必须有真实日志或档案证明模型确实参与了决策并产生了有效游戏动作。模型超时、空输出、非法 JSON 或异常时，规则层必须继续运行，不能让游戏停摆。

### 2. 坦克和友军单位脱困

先复用现有停滞检测、重寻路、侧移和后退机制，找出真实卡住原因。只有确认是友军单位互相阻塞，才实现受限软穿行：

- 只作用于同阵营、同移动域的机动单位；
- 只在确认停滞后启用；
- 只允许短时间、极小位移重叠；
- 建筑、地形边界、敌军和不可通行区域仍不可穿过；
- 脱困后恢复正常避让；
- 必须记录进入、退出和异常情况；
- 禁止全局关闭碰撞，禁止用穿模掩盖导航错误。

## 三、全阶段目标不能删除

U1 侦察、探索和路径恢复；
U2 合法目标、局部交战和小队；
U3 生命周期、响应和去重；
U4 运营和扩张；
U5 真实模型接入及增益；
U6 规模、性能和同条件对照；
U7 用户入口、回归和交付。

所有阶段仍需完成，但可以精简测试数量。不得用函数级测试全部通过替代真实游戏证据。

## 四、精简验收矩阵

必须保留：

- 一条真实模型端到端对局链路；
- 关键移动、生产、交战短局；
- 坦克拥堵和脱困验证；
- 至少一档规模性能对照；
- 核心回归测试；
- 最终正常用户入口验收；
- 模型关闭、模型正常、模型超时/非法输出三种对照。

可以删除或合并：

- 重复覆盖同一逻辑的函数级用例；
- 200 单位极限压力测试；
- 同一问题的重复截图和重复短局；
- 额外案例研究；
- 已被更高层真机烟测覆盖的手工步骤。

## 五、硬性实现约束

- 实时扫描、规则执行和任务恢复不能等待模型；
- 规则层必须在模型关闭时也能持续运营；
- 经济、建造、侦察、军事等线路可并行推进；
- 所有动作必须有 Accepted、Rejected、Arrived、PathFailed、Expired 或等价终态；
- 不得用日志数量、模型响应次数或 JSON 数量冒充有效游戏效果；
- 主力移动必须经过导航、边界、威胁、侦察和安全中继检查；
- 不得用 `attack_move` 绕过安全闸门；
- 真实阻塞时给出证据、已尝试的修复和下一步，不得凭空宣布完成。

## 六、执行顺序

1. 读取当前入口、进度、代码和最近日志；
2. 先完成真实模型闭环；
3. 修复坦克卡住并跑短局验证；
4. 运行精简后的 U1–U4 关键真机验收；
5. 完成 U6 最低必要性能对照；
6. 完成 U7 核心回归和正常用户入口验收；
7. 最后输出报告。

最终报告必须分开列出：已实测通过、代码级通过、仍未验证、明确延期的低收益测试，以及每个延期项的恢复条件。不要把“测试被删掉”写成“功能已验证”。
