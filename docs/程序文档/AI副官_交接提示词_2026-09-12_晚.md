# AI 副官任务交接提示词（2026-09-12 晚 · 直接粘贴给新会话）

你现在接替上一会话继续完成 AI 副官（`G:\AIRTS\AI_RTS`，Godot 4.7.1 **Mono**）的诊断、修复、测试与迭代。
**不要只写分析、不要停在方案、不要做一半等我说"继续"、不要频繁停下汇报。** 长任务请用**后台进程 + 日志**一口气跑完。

---

## 0. 先读（项目规范与用户要求优先；混合架构纠偏覆盖旧实现条款）

1. `docs/项目统一规范.md`（Canonical：Godot 4.7.1 Mono、.NET 8.0.423、main 为唯一基线、DoD、报告规范"只完成哪层就报哪层"）
2. `docs/程序文档/AI副官_当前执行入口.md`（**Canonical 入口**：唯一实施依据顺序、测试口径、旧方案已归档不得复活）
3. `docs/程序文档/AI副官_单模型高效指挥_计划与DeepSeek执行提示词_2026-09-11.md` + `…_技术实现设计_2026-09-11.md`
4. `docs/程序文档/AI副官_传统AI基线与升级路线_2026-09-12.md`（**规则为地板 + 单一权威链**）
5. `docs/程序文档/AI副官_OpenYRWeb源码借鉴_2026-09-12.md`（执行层线索：UnitOrderTrait / TaskRunner / GatherOreTask / AttackMoveTask / ReplayRecorder / PathFinder）
6. `docs/程序文档/AI副官_单模型高效指挥_迭代记录.md`、`AI副官_单模型高效指挥_阻塞报告_2026-09-12.md`
7. `.codebuddy/skills/airts-godot-control-channel/SKILL.md`（**起对局/操作游戏的唯一正确流程**，含 3 个致命坑）
8. `docs/程序文档/AI副官_混合架构纠偏提示词_2026-09-12.md`（**当前最高优先级：规则地板常驻，模型只做高层稀疏 patch**）

## 1. 目标（不是"模型看起来会思考"）

让副官在**实际红警 2 对局**中的有效控制效果**不低于传统 RTS 行为树基线**（游戏内 `source/match/players/simple-clairvoyant-ai/`，含 Economy/Offense/Defense/Intelligence/ConstructionWorks/AutoAttackingBattlegroup）。
**在测量可信之前，任何 A/B 结论都不算数。**

## 2. 用户已拍板的边界（不要重问）

- 测试环境：**本地调试不开电脑 AI 敌人**；Player_0 的"副官 vs 规则 AI"对照**允许**（那是在比较控制器），敌方槽保持空置/被动/脚本化。
- 节奏：小改动 **10 秒短复检**，阶段集成 **5 分钟对局**；**保持对局连续运行**，不要一失败就收摊。
- 模型输出：**继续用极简四列** `{"u":[[执行者,任务,目标,参数]]}`；`decision_id/based_on_snapshot/created_at/expires_at/decision_generation` **由程序生成**；`rationale/confidence/unknowns` 只进日志，**不要挤 96 token 决策预算**。
- 生命周期字段：**双侧记录、单一事实来源**——协调器管决策账本，Godot 权威端管执行事实（`task_id/unit_id/request_id/decision_generation/accepted_at/effective_at/状态/完成证据/失败与阻塞原因`），**协调器不得自行推断"命令已成功"**。
- 已授权游戏侧改动（仅限）：① `construct` 路由（己方未完工建筑 → `gateway.ConstructUnits`，保留占用与合法性检查、**不做无限重试**、记录接收/生效/失败）；② 控制器 A/B 开关（可逆、默认不变、日志记录实际控制器）。
- **不提交 git**（除非明确要求）；GUI 参数按 skill；清理残留**只按端口 PID 或明确属于本任务的 PID**，禁止按映像名批量杀。

### 2.1 用户新增的架构纠偏（硬门槛）

“小模型 + 智能体 + 传统决策树”必须是真正的混合系统。规则中台按独立游戏时间节拍维持默认/有效策略，行为树按执行节拍持续推进；智能体维护任务、调度和反馈；模型只做低频战略、目标、优先级和参数调整。模型没有新结果时不能吞掉本轮 baseline 候选或暂停已有任务，不要求每个渲染帧全量重规划。

先检查 `nodes.py:_tactical_via_task_patch` 的异步提前返回、`node_tactical_agent` 的模型缺失/冷却清空分支，以及 `rules_fallback.py` 是否真的只在失败时调用。修复顺序必须是：`model=off` 的传统 AI 地板 -> 注入超时/空输出仍持续执行 -> `model=on` 的稀疏 patch 与仲裁 -> A/B 实测增量。不得用模型响应速度、JSON 合法率或命令数量替代真实运营效果。

## 3. 环境事实（照抄，别重新摸索）

- Godot：`G:\AIRTS\godot_mono_471\Godot_v4.7.1-stable_mono_win64\Godot_v4.7.1-stable_mono_win64_console.exe`
- 起局（**本地联机 Demo 拓扑**，测试端口）：
  - 专用服：`--headless --path G:\AIRTS\AI_RTS -- --server --port 24569 --debugport 24572`
  - 客户端：`--path G:\AIRTS\AI_RTS -- --debugport 24570 --autojoin --autojoin-lobby --smokehost 127.0.0.1 --smokeport 24569`
  - **`op=start` 必须由客户端发起**（打服务器端口会让客户端留在大厅）；`op=controller {"rule_ai_slot":0}` 是 A/B 开关（默认 -1 不干预），需在 `op=start` **之前**设置。
  - **ENet 走 UDP**（24569 表现为 `UDP 0.0.0.0:24569`，没有 `LISTENING`）；DCS 是 **TCP**（24570/24572）。
  - 本机**没有 `wmic`**（用 `powershell -NoProfile -Command "Get-CimInstance Win32_Process …"`）。
- 现成脚本（都在 `G:\AIRTS\tmp_logs\langgraph_recon\`）：
  `dcs_restart.py`（起局+自检）、`ab_baseline.py`（**A/B 长任务**，日志 `ab_baseline.log`，结果 `ab_result_*.json`）、
  `check_run.py`（回执/重复率/微操/战况速查）、`measure_first_dev.py`（**首发展时延**）、
  `attach_runner_fourcol26_auth.py`（起 runner，**权威端口必须 24572**）。
  仓库内：`source/adjutant_coordinator/deploy/metrics_report.py`（指标采集，**显式列出测不出的项**）。

## 4. 已完成（有实测证据，别重做）

- 四列短输出 + 异步提交 + 逐行校验 + 仲裁合并（`graph/task_patch.py` / `pydantic_agents.py` / `nodes.py`）。
- **权威端复核端口**：`agent_runner --authority-port` 必须指 **24572**（24570 的账本是客户端账本，永远 `PendingAuthority`）。
- 游戏侧幂等账本加**淘汰**（原来满 256 后永久 `LedgerFull`，实测 273/275 条被拒）。
- 进度语义：看不到进展/脱离视野 → **unknown**（不再误判 failed 释放单位引发重发）。
- `construct` 路由已实装并**实测生效**：`op=construct` 出现、**`vehicle_factory constructed=True`**（此前 `Rejected/Occupied ×32` 死循环）。
- 建造落点：半径 4/6/8m（历史证据：5m 能建成、8m+ 全被拒 `NotVisible`）、净空阈值 3.5m、**被拒换点**（`blocked_build_spots`）、**连拒 3 次退避 900 tick**（GPT 要求：不得用无限重试掩盖拒绝）。
- 采集分配器（照抄传统 AI）：**人少优先 + 同拥挤取最近 + A/B 类型均衡** → `behavior_tree._gather` 用预分配结果。
- 控制器 A/B 开关（`NetSession.force_rule_ai_slot` + `op=controller`，默认不变）。
- 已加**接收时间戳**（`received_tick/received_ts` + `command_timing` 日志）。
- 单测：`cd source; $env:PYTHONPATH="G:\AIRTS\AI_RTS\source"; python -m unittest discover -s adjutant_coordinator/tests` → **404 OK**（改动后必须复跑）。

## 5. 未完成（下一步，按此顺序）

1. **修测量本身**（当前最大问题，A 组跑出 `命令=0`、`单位={'':4}`、余额恒 50000）：
   - 服务端 `op=tactical` 的 `unit_type` 是**空串** → 兵种构成改从**客户端 24570** 取，或 `op=rules` 反查；
   - 采样窗口必须**等观察到 ≥1 个己方单位后**再计 3600 tick（skill 记录：开局单位装载在导航烘焙 await 之后，早发命令会被 `EmptyUnitSet` 拒）；
   - 命令计数要**窗口前后各查一次** `op=commands`，并读 `ab_<tag>/runner.out` 判定"没派"还是"没记到"。
2. 补 **effective 时间戳**（progress 按观测证据落）与 **权威端生命周期字段**（第 4 条）。
3. **重跑 A/B**（同图同起点同窗口、只换控制器）→ 用 `metrics_report.py` 并排输出 → **指出差距最大的那一项**。
4. **只修那一项** → 10 秒复检 → 阶段 5 分钟集成。
5. 然后补：**闲置补位**（`EconomyController._assign_idle_workers_to_resources`）、**扩张选址**（`_find_expansion_site`），并验证界面事实：**同一矿点 ≤2 人**、**有钱时不存在无任务的可采集单位**。

## 6. 已踩过的坑（新会话别重犯）

| 坑 | 表现 | 真相 |
|---|---|---|
| GDScript 写 `var s = str(a) or str(b)` | `Parse error` → **DebugControlServer 加载失败 → 调试口不监听 → 对局起不来** | GDScript `or` 是布尔运算符，字符串回退用 `if s.is_empty()` |
| 用 `LISTENING` 查 24569 | 误判"服务端没起来" | ENet 是 UDP |
| 残留 Godot 占端口 | 新服 `listen failed: 20`、客户端"服务器断开" | 起服前按 PID 清残留 |
| 给服务器端口发 `op=start` | 服务器自己开局、客户端留在大厅 | 必须由客户端发起 |
| 同一坏落点无限重试 | `NotVisible` 连拒 31~64 次 | 要"换点 + 退避"（已实现） |
| 服务端 `tactical.unit_type` 空串 | 兵种构成统计全成 `''` | 从客户端取或反查规则 |
| 3600 tick 窗口双方余额都 50000 | 误判"都没发展" | 窗口太早，需等就绪后再计 |

## 7. 报告要求（用户明确）

区分**已实测 / 代码推断 / 尚未验证**；不得用"一次胜负、一次成功对局、模型输出看起来合理"宣称超过传统 AI；每次改动后 10 秒复检并保持对局运行；每阶段 5 分钟集成；最终报告含：环境与固定条件、基线实现方式、副官实现方式、前后指标对比、失败案例与根因、改动文件、行为差异、未解决问题、下一轮最有价值实验。
