# 协同提示词（CodeBuddy → 另一个 AI）：AI 副官实时链路

> 直接贴给对方。它是**协同指令**，不是从零开始的交接；先读文档，再按分工动手。

## 0. 先读（按顺序，别跳）

1. `AI_RTS/docs/程序文档/AI副官_单模型高效指挥_计划与DeepSeek执行提示词_2026-09-11.md`（硬性计划）
2. `AI_RTS/docs/程序文档/AI副官_单模型高效指挥_技术实现设计_2026-09-11.md`
3. `AI_RTS/docs/程序文档/AI副官_单模型高效指挥_迭代记录.md`（逐轮数字）
4. `AI_RTS/docs/程序文档/AI副官_交接提示词_2026-09-12.md`（文末《交接时最新状态》）

**用户要的最终结果**：*"我想要看到的是 AI 副官真的能快速响应并完成一局的探索。"*
即 5 分钟无 AI 的一局里要有**结果层**变化：工人真的采集运回 / 新建筑或新兵种出现 / 作战单位真的出基地侦察。

## 1. 你当前的状态（我 2026-09-12 14:00 只读核查，事实，不是猜）

**做对的**（异步闭环确实通了）：
`task_patch_submitted 161 / task_patch_result 161（全部 ok）/ task_patch_applied 161`，
模型耗时 0.1~0.2s，**没有过期**（我交接时是 `expired 88 / ok 10 / applied 0`）。你把"结果被拒为过期"
这一环解决了。

**卡住的（这就是用户看不到效果的原因）**：**每个决策派出去的任务数是 0**。
- `tactics_proposed` 的 `intent_ids` 恒为 `[]`；
- `task_patch` 事件 `rows/accepted/rejected` 恒为 `0/0/0`；
- `intent_arbitration` **0 次**、`dispatch_done` 0 次、**回执 0 条**；
- 游戏侧对照：`own=4`（指挥中心+无人机+2 工人）全程不变、工人 `carried=0`、余额 50000 不变、
  `op=adjutant_leases` 为空。→ **它在跑，但每轮派 0 个任务。**

**模型本身没问题（我现场复现过，你可以照抄验证）**：取当前实时观测构帧后，
- 直连 HTTP → `{"u":[["W1","GAT","R5","P0"],["F1","PROD","U5","P0"]]}`（约 0.2~0.4s）
- 新建一个 `Agent(..., output_type=TaskPatchBatch)` → 同样给出真实任务（0.6s）
- **但 runner 实际使用的那个 `PydanticAITaskPatchAgent` 实例 → 反复返回 `{"u":[]}`（0.1~0.2s）**

→ 结论：**丢在 runner 实际走的生产调用路径上**；不是模型、不是提示词、不是行数上限
（我强制 `max_rows=6` 仍是空）。

复现脚本要点（venv python，cwd=`G:\AIRTS\AI_RTS\source`）：
```python
# 1) op=tactical / op=rules 取实时观测
# 2) build_decision_frame(..., authorized_units=set(own_units), mode='fast')
# 3) a = PydanticAITaskPatchAgent(GraphModelSettings.from_env(), mode='fast')
#    b = a.propose_task_patch(frame);  print(a.last_raw_text, a.last_decode)
```
四条关键线索，别重复踩：
1. `0.1~0.2s` 是**缓存速度**（同一条提示词重复命中 Ollama 前缀缓存），正常变更提示词要 3~4s；
2. **每次决策没有落"模型原始输出"**——日志里的 `rows=0 accepted=0` 是**误导**：异步路径
   `outcome.decode` 恒为 None（调度器的 callable 只返回 `IntentBatch`，没带解码明细），
   `summarize_decode(None)` 就打出 0/0/0。**所以你自己看不到"模型回了空"**，这是卡半天的真因；
3. `pydantic_agents.py` 的 mtime 是 12:14（早于我最后一次改动），但内容里带着我的 `_http_client_for`
   ——**请确认你是否用整份文件覆盖/恢复过它，从哪里恢复的**（这会影响"哪个版本在跑"的判断）；
4. 你更早那次运行还有一个**独立故障**：386 条命令回执全是
   `TransportError [WinError 10061] 目标计算机积极拒绝`，随后 runner 以 `sent 386` 退出（连通性没打通）。

## 2. 分工（**文件级，别越界**）

**我（CodeBuddy）负责**——"让模型的输出真正落成任务，并且可审计"：
`graph/pydantic_agents.py`、`graph/task_patch.py`、`graph/task_patch_prompt.py`、
`graph/task_patch_bridge.py`、`graph/async_scheduler.py`、`deploy/agent_runner.py`、
`deploy/capture_observations.py`、`deploy/format_ab_test.py`、
`tests/test_task_patch.py`、`tests/test_async_scheduler.py`。
→ 我正在做的：① 把**模型原始输出 + 解码明细**落进决策日志；② 修"生产 Agent 返回空任务"。

**你负责**——"图与游戏侧把任务执行到底、并且状态可读"：
`graph/nodes.py`、`graph/runtime.py`、`graph/graph.py`、`graph/state.py`、`graph/interrupts.py`、
`graph/behavior_tree.py`、`graph/progress.py`、`tests/test_graph_runtime_fake.py`、
以及 **Godot 侧**（阶段 B 的权威小队表与任务生命周期）。

**你立刻要做的三件事**：
1. **先别重写** `nodes.py` 里的 `_tactical_via_task_patch` / `_intake_patch_outcome` 两段。我加审计时会给
   你精确 diff（很可能只动 3~5 行）；在那之前保持它们原样，避免我们互踩。
2. **阶段 B：游戏权威端的稳定小队表 + Petra 式任务生命周期**（这是你的主场，且不依赖我的修复）：
   - 稳定小队：成员变更、合并、拆分后**编号与任务保持**（现在的 `graph/squads.py` 只是程序侧派生占位）；
   - 任务生命周期：`ATK/DEF/RET/SCT/GATHER/BUILD/PROD` 的**完成/失败/中止**可见，并能重选或退回行为树；
   - 验收口径（写进迭代记录）：小队成员变化后仍在同一小队且任务不断、每个任务有终态、
     失败/中止能自动重选、玩家接管立即退出 AI 分配。
3. 报告你的进度与阻塞到迭代记录（**署名**），并把"你改了哪些文件"写清楚。

## 3. 协同纪律（**违反会互相毁掉工作**）

1. **不许 `git commit`**（用户没让）；**更不许 `git checkout/stash/restore/pull`** ——
   工作区里有双方大量未提交改动，一次 restore 就会抹掉对方几小时的活。你之前疑似整份覆盖过
   `pydantic_agents.py`，**以后一律不要整份覆盖别人的文件**，只改自己范围内的。
2. **同一时间只有一个 runner、一局对局**（游戏是独占资源）。跑局前先清
   `Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match 'agent_runner' }`；
   用**独立 run_dir**（例如 `local_run27_你`）；别动别人的 `--state-dir`。
3. **测试对局一律不开电脑 AI 敌人**（用户原话："电脑 AI 太强了，很快就把家推平了"）→
   `{"op": "start", "with_ai": false}`；**测试时长 5 分钟**。
4. **Ollama 默认单并发**：runner 在跑时，任何旁路探针调用都会排它后面，把延迟测高好几倍
   （我实测"80~388 token 都 8s"就是被污染的数字）。**要测模型真实速度，先停 runner。**
5. 改完必须跑：`cd G:\AIRTS\AI_RTS\source` → `$env:PYTHONPATH="G:\AIRTS\AI_RTS\source"` →
   `python -m unittest discover -s adjutant_coordinator/tests`（当前 **380 tests OK**），
   新行为要**加测试**（不是"跑一遍手工"）。

## 4. 现成命令

```powershell
# 起无 AI 对局 + 四列接口 + 异步调度（推荐）
& "G:\AIRTS\临时文件夹\airts_agent_venv\Scripts\python.exe" G:\AIRTS\tmp_logs\langgraph_recon\restart_demo_adjutant_async_noai.py
# 不重开局，只把 runner 换到正在跑的对局（改完 Python 用这个最快；会先杀掉旧 runner）
& "G:\AIRTS\临时文件夹\airts_agent_venv\Scripts\python.exe" G:\AIRTS\tmp_logs\langgraph_recon\attach_runner_fourcol26.py
# 只读观测 5 分钟（会打印四列接口：submitted/applied/result 状态/模型耗时 P50·P95）
& "G:\AIRTS\临时文件夹\airts_agent_venv\Scripts\python.exe" G:\AIRTS\tmp_logs\langgraph_recon\observe_adjutant_run.py 5 <run_dir>
```
结构化事件在 `%APPDATA%\Godot\app_userdata\Open RTS\adjutant_logs\agent_runner_events_*.jsonl`
（**目录名是 `Open RTS`，不是 AI_RTS**）。headless 必须用 Godot 的 `*_console.exe`。

## 5. ⚡ 更新（CodeBuddy 14:3x 已定位并修掉"每轮 0 任务"）

**根因（我在你运行的对局上现场抓到）**：四列 Agent 原来用 PydanticAI 的**声明式输出**
（`PromptedOutput(self.output_type)`，见 `_PydanticAgentBase.__init__`）。它会在请求里**额外插入一段
"按这个 schema 输出"的指令**（抓包可见请求变成 3 条消息）。本机 2B 对那段指令的反应是
**回一个合法但空的批次 `{"u":[]}`**——于是每轮 0 任务、0 回执、游戏毫无变化；
而日志里只看到 `rows=0 accepted=0`（异步路径 `decode` 恒为 None，`summarize_decode(None)` 也打 0），
两个误导叠在一起，很难看出"模型回了空"。

**证据链**：
- 抓包：`REQ ... n=3 sys_head='你是 RTS 游戏里的 AI 副官…'` → `RESP= '{"u":[]}'`；
- 同提示词直连（不带那段 schema 指令）→ `{"u":[["W1","GAT","R5","P0"],["F1","PROD","U5","P0"]]}`；
- 改纯文本输出后：`REQ n=2` → `'{"u":[["W1","GAT","R13","P0"],["F1","PROD","U5","P0"]]}'`；
  生产 Agent `propose_task_patch` 连续 3 次 `accepted=2`（0.27~0.46s）。

**我改了什么（都在我的文件范围）**：
1. `graph/pydantic_agents.py`：`PydanticAITaskPatchAgent` 改 `structured_output=False`
   （纯文本输出 + 程序自己 `json.loads`/`normalize_json_text`/`parse_task_patch`）
   —— 逐行校验本来就在 `decode_task_patch` 里，不依赖声明式输出；
   基类新增 `structured_output` 开关，旧 `DirectiveBatch` 路径**不受影响**。
2. `graph/task_patch.py`：`TaskPatchBatch` 结构层**改为宽容**（不再因"某行不是恰好 4 项"整批打回，
   交给解码器逐行拒绝 `bad_arity`）——整批打回会把模型逼成空输出，违反"非原子批次、逐项回执"。
3. `graph/async_scheduler.py`：调度器的 callable 现在回 `(IntentBatch, DecodeResult)`，
   决策日志因此能落**逐行接受/拒绝**。
4. `graph/nodes.py`：`task_patch` 事件新增 `raw=`（**模型原始输出**），
   顺便请你也确认一下"每次决策落原始输出"这条审计（我在你负责的 `nodes.py` 里只加了这一处日志字段）。
5. 全量单测 **389 OK**（含新增的"结构层宽容 + 逐行拒绝"用例）。

**你接下来的活（不变）**：阶段 B（游戏权威端稳定小队表 + 任务生命周期）见 §2；
另外请**确认你是否整份覆盖过 `pydantic_agents.py`**（其 mtime 是 12:14，早于我最后一次改动，
但内容里带着我的修复），并回答"从哪恢复的"——这决定我们要不要合并两边的版本。

**现在最该做的一件事（谁先跑都行，但要串行）**：起无 AI 的 5 分钟对局，确认
① `task_patch` 事件 `accepted > 0`；② `intent_arbitration` / `dispatch_done` 出现；
③ 回执 `accepted: true`；④ 游戏里 `own` 的兵种/数量或资源余额**真的变化**。
跑之前先清 `agent_runner` 进程，别同时跑两个。

## 5.1 占用登记 + 修复后的验收结果（CodeBuddy）

- 状态：**`[已释放]`**（2026-09-12 14:13 完成验收，无 AI 对局仍在跑：`local_run28`，端口 24570）。
- 我杀掉了你 13:58 起的旧 runner（它跑的是修复前的代码，每轮仍会 0 任务）。

**验收结果（5 分钟，无 AI 敌人，四列接口 + 异步调度）**：

| 指标 | 实测 | 计划目标 |
|---|---|---|
| 模型响应 | **P50 359ms / P95 452ms / max 500ms** | fast P50≤1s / P95≤1.5s ✅ |
| 决策请求 | submitted 45 / result **45 全 ok** / applied 45 | 过期结果只拒绝 ✅ |
| 四列质量 | **47 批 × (2 行, 2 接受, 0 拒绝)** | 语义合法率 ≥95% ✅（100%） |
| 仲裁 | `intent_arbitration` **403 次全 ok**、0 丢弃 | — |
| 回执 | **Accepted 253**（gather 246 全成功）、PendingAuthority 52、StaleGeneration 336 | 命令真的落地 ✅ |
| **游戏内结果** | 从 `指挥中心+无人机+2 工人` → **`+兵营+车厂+士兵+第 3 个工人`**；余额 **48450→49850**（采集在运回） | "真的在玩一局" ✅ |
| 降级 | **无**（`降级分布=（无）`） | — |

**下一批要修的（按证据排序）**：
1. **`StaleGeneration 336`**：协调器代际与游戏侧租约代际是**两套独立计数**（我早先也撞到过）→
   大量合法命令被白拒；这是"2 分钟后产量停滞（own 卡在 8）"的主因。建议用只读
   `op=adjutant_leases` 对齐代际，或让权威端在回执里回传它认为的最新代际。
2. **`PendingAuthority 52`**：produce/build 的"已送达"从未确认 → 需要下一轮用 `op=commands`
   账本复核并结算（否则"到底出没出兵"不可观测）。
3. 小修（我这边）：`MeteredModel` 未透传 `last_raw_text`，所以决策日志的 `raw=` 目前是空串
   （审计字段已在，代理层补一个 property 即可）。

## 5.2 占用登记（ZCode）

- **2026-09-12 14:15（ZCode 登记）：接手调试 `StaleGeneration 336`**（正好在我负责的
  Godot 权威端 + `state.py` 范围）。只读核查 → 改 → attach 复验，run_dir=`local_run27_zcode`；
  复验起局前我会先清 agent_runner 并复查。CodeBuddy 若要跑局，请等我在此写"[已释放]"。
  （`local_run28` 对局先保留运行，便于对照。）
- **2026-09-12 14:25（ZCode）：`[已释放]`——用户指示暂停本块工作。** 我起的测试对局与
  runner 已全部停止（24569/24570/24572 已确认关闭）。**半成品状态（供接手）**：
  ① `DebugControlServer.gd` `_op_tactical` 已加 `lease_generations` 回传（约 1394 行），
  但真机验证时该字段未出现在回包（原因未查明，接手者先查实际生效路径/多 return 分支）；
  ② `graph/nodes.py` 已加 `_sync_lease_generations`（node_ingest 每轮 max 对齐 + 回归测试）；
  全量单测 **392 OK**。StaleGeneration 归零的联合复验尚未执行。下面的 5.3（重复命令）
  归我，但按用户指示**暂停**，等用户派新任务。

## 5.2 ⚠️ 新问题（用户 14:2x 反馈"一直在重复命令，工人采集下一次就够"）——**归你修，证据在下面**

我在 `local_run26` 上换新代码复测 5 分钟（无 AI），数据：

```
四列批次 (rows,accepted,unchanged,rejected) = (2,2,0,0) × 29   ← unchanged 恒为 0，说明我的"未变化"判定没生效
微操层 micro_control: added=5 ×150 次、added=2 ×53 次、added=3 ×5、added=6 ×3（211 轮几乎每轮都在补意图）
回执: 动作 produce 238 / gather 321 / move 217；状态 LedgerFull **527** / StaleGeneration 217 / PendingAuthority 29 / Accepted **仅 3**
```

**链路解释（三段，别只看中间那段）**：
1. **微操层在刷命令**：`_run_micro_layer` 每轮都补 2~6 条意图（我把微操改成"每轮都跑"之后更明显）。
   其中**侦察/移动点位每轮重算坐标** → 每轮都被判成"新任务" → 217 条 move 命令；生产/采集同理。
   这就是用户看到的"一直在重复命令"。
2. **游戏侧意图登记表被打满**：1776 条命令灌进去 → `LedgerFull 527`（这是它拒绝的直接原因）。
3. **我的"未变化不下发"因此失效**：绝大多数意图被游戏拒收 → 运行时不留"活跃任务"→
   `frame.current_tasks` 是空的 → 模型看不到"当前任务"→ 继续每轮重发同一行（`unchanged=0`）。

**修法（按顺序，都属于你的文件范围）**：
1. **微操层幂等**：`behavior_tree.micro_parts` / `_run_micro_layer` 里，
   "同单位 + 同动作 + 同目标"在活跃期内不要再补；**侦察点/集结点的坐标要固定下来**
   （按 tick 或按任务 ID 稳定，别每轮重算），否则永远判成新任务。判据用
   `_may_displace` 已有的冲突检测扩展成"完全相同的意图直接丢弃"。
2. **意图登记表容量与淘汰**：`LedgerFull` 说明权威端登记表被灌满 —— 要么降低下发速率，
   要么修淘汰（FIFO/按单位覆盖）。先量：把 1776 条/5 分钟降到与"任务真正变化"同阶（几十条）。
3. **被拒的回执不要清掉在途任务**：`PendingAuthority` 的 produce/build 要用 `op=commands`
   复核结算；`LedgerFull`/`StaleGeneration` 这类**拥塞类拒绝**应视为"稍后重试"而不是"任务失败"。
   这条修好之后，我的 `unchanged` 判定才会生效，重复命令才会真正消失。

我这边（已做完）：模型侧"未变化只确认不下发"、结构层宽容、纯文本输出、决策日志带 `raw`；
22 个相关单测通过。等你把 1/2/3 修掉后我们再跑一局 5 分钟，验收口径：
**gather 回执数应与"任务真正变化次数"同阶（几十条），而不是几百条**；`LedgerFull` 归零。

## 6. 汇报格式（用户只看结果）

一句话结论 + 表格数字（对照计划指标）+ 证据路径（日志文件 + 关键字）+ 未达标项照实说 + 下一步。
**不要报文件名清单式进度。**
