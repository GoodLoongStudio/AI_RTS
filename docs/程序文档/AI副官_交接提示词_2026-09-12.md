# 交接提示词：AI 副官「单模型高效指挥」改造（2026-09-12）

> 本文件为历史接续记录；当前执行先读[当前入口](AI副官_当前执行入口.md)与[混合架构纠偏](AI副官_混合架构纠偏提示词_2026-09-12.md)。历史端口、分工和测试数字须复核；“发展阶梯只在降级路径生效”已废止，不得恢复。

> 复制下面整段给接手方（有电脑操作权限的 AI）。它需要能：起/停本地对局、读写
> `G:\AIRTS` 下的文件、运行 Python 与 Godot。**它必须能操作游戏窗口（或用 DCS 只读通道
> 观测），否则阶段 C 的端到端验收做不了。**

---

## 你的任务

严格执行用户交付的正式方案，把 AI 副官改成"**单个本地小模型做低频短决策 + 程序展开 +
有界异步调度 + 行为树高频执行**"：

- 计划（硬性）：`AI_RTS/docs/程序文档/AI副官_单模型高效指挥_计划与DeepSeek执行提示词_2026-09-11.md`
- 必读设计：`AI_RTS/docs/程序文档/AI副官_单模型高效指挥_技术实现设计_2026-09-11.md`
- **已完成的逐轮记录（先读它，能省你几小时）**：
  `AI_RTS/docs/程序文档/AI副官_单模型高效指挥_迭代记录.md`

工作方式（用户明确要求）：连续交付，**不要每步停下等"继续"**；只在需要付费、不可逆
操作或缺少凭据时暂停。每轮改动都要有**可复核的数字**，不许美化、不许把"候选增加后没变化"
当证据。**最终只在可执行验收项全部完成后才汇报**；若上下文将耗尽，先把迭代状态与下一步
入口写进上面的迭代记录，再继续。

## 用户已明确的纪律（违反=返工）

1. **测试对局一律不开电脑 AI 敌人**（用户原话："电脑 AI 太强了，很快就把家推平了"）
   → `op=start` 用 `{"with_ai": False}`。
2. **测试对局时长一律 5 分钟**，不要自行加长。
3. **不要擅自提交 git**。工作区有大量未提交改动，是故意的。
4. 结论必须带证据（日志字段、回执、命令计数），并说清"用户屏幕上应该看到什么"。
5. 完成实质工作后，向 `g:\AIRTS\.codebuddy\memory\YYYY-MM-DD.md` 追加简报，
   长期约定写进 `g:\AIRTS\.codebuddy\memory\MEMORY.md`。

## 环境（照抄，别猜）

| 项 | 值 |
|---|---|
| 游戏仓库 | `G:\AIRTS\AI_RTS`（Godot 4.7 Mono） |
| Python 协调器 | `G:\AIRTS\AI_RTS\source\adjutant_coordinator`（包名 `adjutant_coordinator`） |
| Python 解释器 | `G:\AIRTS\临时文件夹\airts_agent_venv\Scripts\python.exe`（**必须**用它，含 pydantic-ai/langgraph） |
| Godot（headless 必须用 console 版） | `G:\AIRTS\godot_mono_471\Godot_v4.7.1-stable_mono_win64\..._console.exe` |
| 模型 | `.env.local` 里配置；当前 `minicpm5-adj-16k`（Ollama，2.5B，Q8_0，ctx 16384），端点 `http://127.0.0.1:11434/v1` |
| DCS 调试端口 | 专用服 24569（DBG 24572）、可见客户端 DBG 24570 |
| HUD/日志目录 | `%APPDATA%\Godot\app_userdata\`**`Open RTS`**`\adjutant_logs\`（**目录名是项目名 `Open RTS`，不是 AI_RTS**；面板与结构化日志都读这里） |
| 单测 | `cd G:\AIRTS\AI_RTS\source` → `$env:PYTHONPATH="G:\AIRTS\AI_RTS\source"` → `python -m unittest discover -s adjutant_coordinator/tests`（当前 **380 tests OK**） |

### 现成的脚本（`G:\AIRTS\tmp_logs\langgraph_recon\`）
- `restart_demo_adjutant_async_noai.py`：**起无 AI 对局 + 副官用四列接口/异步调度**（推荐入口）
- `attach_runner_fourcol26.py`：**不重开局**、只把 runner 换到正在跑的对局上（改 Python 后用这个最快）
- `observe_adjutant_run.py <分钟> <run_dir>`：只读观测，结束时汇总路由/降级/回执/四列接口统计
- `capture_observations.py`（在仓库里）：只读采集真实观测，供离线 A/B

启动 runner 的关键参数（顺序无所谓）：
```
--authority-port 24570 --provider real --engine langgraph --allow-other-port
--player Player_0 --interface four-col --tactics-mode fast --scheduling async
--strategy-mode off --max-batch 24 --tactics-interval 120 --event-interval 120
--env-file <仓库>/source/adjutant_coordinator/.env.local
--state-dir <run_dir> --log-dir "%APPDATA%\Godot\app_userdata\Open RTS\adjutant_logs"
--pidfile "%APPDATA%\Godot\app_userdata\Open RTS\adjutant_logs\agent_runner.pid"
```
`--log-dir`/`--pidfile` 写错目录 = HUD 永远显示"副官尚未启动"（而 runner 其实在干活）。

## 已经做完的（阶段 A / A-2 / C 前半）

1. **四列短决策接口**（`graph/task_patch.py`、`task_patch_prompt.py`、`squads.py`）：
   模型只输出 `{"u":[[actor,skill,target,params]]}`，程序做引用表/能力/目标/参数校验。
   120 条真实观测 A/B：fast 语义 **99.8%**、延迟 **P50 0.55s / P95 0.83s**、输入 918 token；
   旧格式 98.0% / P50 1.20s / P95 4.11s / 输入 4350。deep 语义 99.0% / P95 0.91s。
   结果：`source/adjutant_coordinator/logs/format_ab_both_20260912.json`
2. **接入运行时**：`PydanticAITaskPatchAgent`（唯一决策入口）、`task_patch_bridge.py`、
   `nodes._tactical_via_task_patch`；旧 `propose_intents` 路径保留做 A/B 与回退。
   历史实现曾将发展阶梯限于降级路径；该设计已废止。当前要求规则中台常驻，默认策略独立运行，模型只修改高层策略与任务，来源分别记录。
3. **有界异步调度**（`graph/async_scheduler.py`，13 个单测）：单一在途工作槽、普通观测合并、
   紧急插队、有界队列、**过期/观测过旧/代际过期一律拒绝**、单调时钟失败退避。
   结果由 `node_classify` 的 `_intake_patch_outcome` **每轮即时收取**（`patch_ready`）。
4. **已修的两个真 bug（都别再犯）**：
   - **代理陷阱**：Windows 注册表里配了系统代理（`ProxyEnabled=1 / 127.0.0.1:7897`）时，
     `httpx`（PydanticAI 用）**不处理 Windows 的 localhost bypass 列表**，于是访问本机
     Ollama 的请求被送去代理 → **502 空响应**（Ollama 日志里看不到该请求）。
     修法：`pydantic_agents._http_client_for()` 对本机端点用 `httpx.AsyncClient(trust_env=False)`。
     **症状识别**：模型调用报 `status_code: 502` 且 body 为空。
   - **全局在途标记**：原先模块级 `_INFLIGHT` 让战略调用阻塞战术调用（实测 6 次
     "上一轮推理仍在进行中"）。已改为每 Agent 一份。

## 现在卡在哪（**接手后的第一件事**）——⚠️ 以文末《交接时最新状态》为准

最后一次真实对局（无 AI，四列+异步）的观测结果：模型只用 **250ms** 就回了，
但 **6 次请求 6 次 `expired`，一条命令都没下发**。根因已定位并已改：

- 原因：结果**只在"战术路由"才被取走**（间隔约 26s），远超 fast 的 1.5s 本地接收期限。
- 已改：`node_classify` 每轮即时收取（`_intake_patch_outcome`），有结果就把路由切到战术节点落地；
  同时按计划阶段 A 的允许项**关闭战略层**（`--strategy-mode off`；实测它 18 次
  `Exceeded maximum retries` 同步阻塞主循环，把每轮拖到 4.7s）。
- **此修复尚未在真实对局里验证**（最后一次观测被用户取消）。你要做的第一条验收：

```
python tmp_logs/langgraph_recon/attach_runner_fourcol26.py      # 或 restart_..._noai.py 重开
python tmp_logs/langgraph_recon/observe_adjutant_run.py 5 <run_dir>
```
看**四列接口**那一行与结构化日志（`task_patch_result` / `task_patch_applied`）：

| 验收项 | 通过口径 |
|---|---|
| 结果被接受 | `task_patch_result.status == ok`（不再全是 expired），并出现 `task_patch_applied` |
| 命令真的下发 | 回执里有 `accepted: true`；`own` 单位数/兵种在 5 分钟内**发生变化**（例如多出工人/建筑） |
| 端到端时延 | 从"事件进入"到"单位进入新任务"：用 `latency_ms` + 轮询间隔估算，记进迭代记录（计划要求 fast P50≤1s/P95≤1.5s、deep P50≤2s/P95≤3s） |
| 主循环不被阻塞 | 模型在途期间 `graph_observation` 仍按 ~0.5s 节奏增长（`--tick-interval 0.5`） |
| 观测合并 | 高负载时 `merged > 0`；`expired` 只出现在**真的**超过期限时 |
| 不推断状态 | 超时/失败期间 `active_intents` 保持、既有任务继续（不得因超时改写任务状态） |

如果仍然 expired：**先量再改**——查 `waited_ms`/`latency_ms` 与轮询间隔，确认是"轮询太慢"
还是"模型太慢"，再按证据调期限或轮询（计划允许依据实测调整，但**必须写进迭代记录**）。
不要用"放宽期限掩盖慢推理"的方式过验收。

## 之后按计划顺序做（不要跳）

1. **阶段 C 剩余**：优先级队列（紧急>普通>对账）、复盘模式入口、端到端响应测量与记录。
2. **阶段 B（游戏权威端）**：稳定小队表（成员变更/合并/拆分也保持编号与任务）与
   Petra 式任务生命周期（ATK/DEF/RET/SCT/GATHER/BUILD/PROD 的完成/失败/中止、需要时重选或返回行为树）。
   现在小队是**程序侧派生**（`graph/squads.py`），只是占位。
3. **阶段 D**：把"上下文/提示词/候选/决策与回执日志/验收案例"做成数据集，做一次小规模微调，
   再用 **100/200/300 单位**的观测与整局验收（判据：命令回执成功率 ≥99.5%、
   无"收到不执行"、指令响应达到第 3 节指标、玩家接管立即生效、结束可复盘）。

## 别做的事（安全边界，计划明令）

- 不许让程序**自行扩张/换兵种/改全局战略**（模型没选的任务不得执行）；
- 不许在结果到达时把"发起时的观测/代际"换成最新值（过期结果**只拒绝**，不刷新）；
- 不许用"冻结单位/单位满/任务满/拒绝新命令"来压指标；
- 不许让**第二个 LLM** 进入实时决策回路（模型选择直接落地，仅做确定性校验与展开）；
- 不许把推理超时当作"单位或任务状态"的依据（状态只能来自权威端与回执）；
- 玩家手动接管的单位必须**立刻**退出 AI 分配（`player_control_released` /
  `UnitActionsController._release_adjutant_leases()` 是玩家优先权的实现，别删）。

## 已知的坑（省你时间）

| 症状 | 真因 |
|---|---|
| 模型调用 `status_code: 502` 空响应 | 系统代理拦截 127.0.0.1（见上，`trust_env=False` 已修） |
| HUD 恒显"副官尚未启动" | `user://` 目录名是 `Open RTS`；runner 的 `--pidfile`/`--log-dir` 必须指向那里 |
| headless 测试"0 字节日志 + exit=0 假通过" | 用了 GUI 版 Godot，必须用 `*_console.exe` |
| `cd adjutant_coordinator` 跑 discover 时 `test_behavior_tree` 报找不到包 | 运行目录问题：要在 `source/` 下跑并设 `PYTHONPATH=source` |
| 面板"预留"行永远"等待对局" | HUD 硬编码连 24579，而测试客户端在 24570，不是故障 |
| 想验证"副官是否在打仗" | 别用血条（自动攻击使归因失效）：用命令回执 + `op=combat` 开火计数 + 结果层（建筑/兵种真的出现） |

## 汇报格式（用户只看结果）

一句话结论 + 表格数字（对照计划指标）+ 证据路径（日志文件、structured jsonl 关键字）
+ 未达标项照实说 + 下一步。**不要报文件名清单式进度**，用户明确说过"文件冲突跟我说有啥用"。

---

# 交接时最新状态（2026-09-12 12:45 交接，以本节为准）

## 用户要的最终结果（验收口径，别跑偏）

> 用户原话意图：**"我想要看到的是 AI 副官真的能快速响应并完成一局的探索。"**
> 即：5 分钟无 AI 的一局里，副官要能**快速响应**（计划 §7.C 的响应指标）并且**真的在玩**：
> 工人去采集、有建筑/兵种产出、作战单位去侦察/前压（结果层证据，不是"发出了命令"）。

## 工作区状态（可直接开工）

- 全量单测 **380 tests OK**（`cd G:\AIRTS\AI_RTS\source`，`PYTHONPATH=source`，
  `python -m unittest discover -s adjutant_coordinator/tests`）；**未提交 git**（故意的）。
- 本轮我最后改了两处并已跑过单测：
  1. `deploy/agent_runner.py`：关掉的模型层必须返回 `None`，不能包成"里层为 None 的空壳
     `MeteredModel`"（这个空壳导致 46 次 `'NoneType' object has no attribute 'propose_plan'`
     降级 → 冷却 → 战术节点被跳过 → **`task_patch_applied = 0`**）；
  2. `graph/nodes.py`：新增 `_model_missing()` 防御（`None` 或包装器里层为 `None` 都算不可用），
     战略/战术两道守卫都改用它；`node_classify._intake_patch_outcome()` **每轮即时收取**异步结果。

## 最近一次真机数据（无 AI，6.5 分钟，四列接口 + 异步调度）

| 指标 | 实测 | 说明 |
|---|---|---|
| 主循环 | `graph_observation 677` / 6.5min ≈ **0.58s/轮** | **已不再被推理阻塞**（`--strategy-mode off` 生效） |
| 决策请求 | `task_patch_submitted 151` | 合并机制在跑 |
| 决策结果 | `task_patch_result 98`：**expired 88 / ok 10** | 期限（fast 1.5s）远小于实际模型耗时 |
| 落地应用 | **`task_patch_applied 0`** | 主因＝上面的空壳包装器 bug（已修，**未复验**） |
| 模型耗时 | 服务端日志 **3.5~4.4s**；P50 4.2s、max 8s | 计划要求 fast P50 ≤1s / P95 ≤1.5s |
| 战况 | `own=4` 全程不变（`command_center 1 / drone 1 / worker 2`） | **副官没有真的在玩——这就是要解决的核心问题** |

### 两个待判定问题（按顺序做，别跳）

**Q1（先做）：模型在"只有游戏在跑"时到底多快？**
注意：我最后那次"干净条件复测"**被取消了**（当时先杀 runner 再连测 5 次）。
已有证据表明 **Ollama 默认单并发**：runner 一直在提交请求时，任何新的探针调用都要排在它后面，
于是我测到"输入 80~388 token 全都是 ~8s"——**这个数字被 runner 自身流量污染了，不可信**。
所以第一步是：**杀掉 runner → 连测 5 次（同规模提示词）→ 记录 P50/P95**。

**Q2：若干净条件下仍 >1.5s**，按证据（并写进迭代记录）选一条或组合，**不许用"放宽期限掩盖慢推理"**：
- a) 按计划本意把 **fast 输入压到"数百 token"**（现在 917 token：8 执行者 + 16 资源 + 6 建筑 + 技能图例，
  远超计划对 fast 的描述"从上一响应以来的一小段时间内的数百个 token"）；
- b) 给 GPU 让路：限制游戏客户端 FPS/vsync（**需用户同意**，属改游戏设置）；
- c) 换更快量化/模型（**必须**在迭代记录里写明基础模型、量化、模板、上下文 —— 计划硬性要求）。

**然后才看 `task_patch_applied > 0`**：结果一旦能落地，就有 `accepted: true` 回执与单位行为变化。

## 照抄命令（接手第一步）

```powershell
# 0) 现在可能仍有旧代码的 runner 在跑（我最后一次清理命令被取消）；下面脚本会先清掉它
# 1) 换到修复后的代码（不重开局；若对局已经不在，用 restart_demo_adjutant_async_noai.py 重开，注意 with_ai=False）
& "G:\AIRTS\临时文件夹\airts_agent_venv\Scripts\python.exe" G:\AIRTS\tmp_logs\langgraph_recon\attach_runner_fourcol26.py

# 2) 只读观测 5 分钟（用户要求 5 分钟；**不要开 AI 敌人**）
& "G:\AIRTS\临时文件夹\airts_agent_venv\Scripts\python.exe" G:\AIRTS\tmp_logs\langgraph_recon\observe_adjutant_run.py 5 G:\AIRTS\tmp_logs\langgraph_recon\local_run26
```
观测脚本现在会直接打印 **四列接口** 一行：提交数、`result` 状态分布、`applied` 数、
模型耗时 P50/P95/max（结构化事件在
`%APPDATA%\Godot\app_userdata\Open RTS\adjutant_logs\agent_runner_events_*.jsonl`）。

通过口径（**缺一条都不算完成**）：
1. `task_patch_result.status == ok` 占绝大多数（不再 88/98 全是 expired）；
2. `task_patch_applied > 0`，且回执里有 `accepted: true`；
3. **结果层**：5 分钟内出现至少两类变化——资源被采集/运回、新建筑或新兵种出现、作战单位离开基地去侦察；
4. 模型在途期间 `graph_observation` 仍按 ~0.5s 节奏增长（主循环没被阻塞）；
5. 超时/失败期间 `active_intents` 不被改写（不得据推理超时推断单位或任务状态）；
6. 端到端响应（事件进入 → 单位实际进入新任务）写进迭代记录，与计划指标对照给结论。
