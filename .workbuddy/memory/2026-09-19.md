
## Pull 撞名第三次+project.godot 写坏（2026-09-19 晚）
- 远端"大更新"(3f795b8e) 带 mapgen/tmp_logs 的 .png.import，与本地 230 个改动相撞。分三类处理：①project.godot 本地修改是**编码写坏的垃圾**（中文注释 mojibake+结构压扁）→ checkout 还原；②192 个 mapgen .import tracked 修改（Godot 重新生成的中间产物）→ checkout 还原远端优先；③37 个 untracked .import 撞名 → 备份 /tmp/ai_rts_untracked_backup3 后 pull。
- ★模式固化：Desktop pull 报 overwritten 时，用 comm(本地改动, FETCH_HEAD来货) 分 M/?? 两类——中间产物/写坏文件还原，有价值改动提交，untracked 备份移除。.cursor 截图修改不撞名，保留。
- pull 后工作区仅剩 5 个 .cursor 修改。
