# AI_RTS 开发交接文档（2026-09-06）

> 写给下一个对话：读这份文档 + .workbuddy/memory/MEMORY.md 即可无缝继续。
> 分支 yyp_test，HEAD = 9f5a9b79。所有提交信息为中文。

## 一、最近完成的功能（已提交，均经自动化测试验证）

| 功能 | 提交 | 要点 |
|---|---|---|
| 步兵期 1+2 全套 | 33e97026 → 2b03e84f | 步兵（Infantry_anim_v2.glb，生产/部署/交战）+ 兵营（Barracks，产步兵）+ AI 步兵副线（NORMAL 4/HARD 8/EASY 关） |
| 阵营色区分 | ec24ca11, b20fd848 | Synty 图集单位走 SyntyMaterialBinder.apply_team_tint；GLB 步兵等无 binder 网格走 Unit._setup_color 的 mesh 级兜底；共享 shader source/shaders/3d/team_tint.gdshader（去饱和+阵营色+保留30%细节）；色板前4色改 2979ff/ff5252/00e676/d500f9 |
| 主基地换模 | 12262b33 | structure-geometries/CommandCenter.tscn → SM_Bld_Pod_Research_05（玻璃穹顶指挥中枢，scale 0.12，y+0.12） |
| 开局配置 | d5ac04a2, 12262b33 | 主基地×1 + 无人机×1 + 工人×2（Match.gd _spawn_starting_units 段），其余全靠工人建造/生产 |
| 联机大厅三连修 | 742c9fef, 6fc0917b, 8af7ba51 | ①点准备后开局（门槛降为≥1人类全就绪）②房主判定 v3（大厅广播 last_lobby_slots 的最小人类槽位；服务器侧 _is_room_owner_slot 同口径）③本机开房（单人对AI）按钮 |
| 联机 NORMAL AI 降速 | 9f5a9b79 | SimpleClairvoyantAI._ready：is_networked 且 NORMAL → workers 5/CC、编组 2×5、first_wave_delay 120s；单机自定义口径不变 |

## 二、待办（按优先级）

1. **Push**：本地领先 origin/yyp_test 一个提交（9f5a9b79 AI 降速），GitHub Desktop 推送即可。
2. **云服部署**（联机大厅修复+AI降速要生效必须更新腾讯云 101.43.121.102 专用服）：
   - 脚本 `dev/deploy_lobby_fix_20260905.py`（paramiko，需用户填 SSH 密码；凭据文件 服务器信息.md 在离线的 G 盘）
   - 增量 bundle 需重打：`git bundle create incr.bundle 51f8f92f..yyp_test`（51f8f92f 后无新增 LFS 资产，纯代码）
   - 流程：SFTP 上传 → 服务器 git pull → systemctl restart airts-game
   - 注意：部署脚本里的 REMOTE_DIR/SERVICE 占位需按服务器实际路径核对
3. **RuleAiAggressionSmokeTest 确定性挂起**：报 "Nonexistent function 'GetSpawnPoints' in base 'Nil'"（AutoAttackingBattlegroup 的 _world_query_runtime 为 Nil），无 completed 输出。stash 隔离验证与 AI 降速无关；昨天还通过 → 疑与并行会话改动（Infantry GLB 替换）或开局单位调整有关。排查方向：battlegroup setup 时序 / world query session 注册 / Match teardown 顺序。
4. **NetInfantrySmokeTest E2E 未打通**：联机冒烟骨架（专用服+客户端）已建（tests/automated/NetInfantrySmokeTest.gd），卡在 headless 客户端 "units" 组为空——NetSync 单位复制到客户端的细节待查，打通后即可全自动验证联机生产链路。
5. **云服代码版本**：云服当前跑 51f8f92f 之前的代码；部署后大厅修复/AI降速才对云服对局生效。

## 三、已知行为（用户已确认的设计）

- 开局 = 主基地×1 + 无人机×1 + 工人×2；其余全靠建造/生产（AI 同样起步）。
- 大厅"立即开局"= 正式对局（AI 会进攻）；"本机开房（单人对AI）"同理；被动测试局钩子保留给 --autojoin 自动化冒烟。
- 阵营色板 P1-P4：2979ff/ff5252/00e676/d500f9；浓淡调 SyntyMaterialBinder.team_tint_strength（0~1）与 Unit.gd 的 team_mix。
- 步兵已由并行会话换成带动画的 Infantry_anim_v2.glb（InfantryAnimationDriver 程序化驱动仍在场景中共存）；阵营色 mesh 级兜底已兼容 GLB。

## 四、验证命令速查

```bash
# Godot 自动测试（窗口版带 --log-file 规避沙箱）：
/d/github_work/Godot_v4.7.1-stable_mono_win64/Godot_v4.7.1-stable_mono_win64/Godot_v4.7.1-stable_mono_win64_console.exe \
  --path . --log-file <本地.log> res://tests/automated/<TestName>.tscn
# C# 核心测试：dotnet run --project tests/core/AI_RTS.Core.Tests.csproj
```
关键测试：InfantrySmokeTest（步兵全链路）、RuleAiOffenseLogisticsSmokeTest（AI 物流）、RuleAiBattlegroup/AggressionSmokeTest、ProductionQueueSmokeTest、StructurePlacementSmokeTest、BalanceConfigRuntimeSmokeTest。

## 五、协作注意

- 并行会话（Codex 等）会改同一 repo：Infantry.tscn/GLB、Match.gd 曾被外部改写导致回归冲突——改文件前先 `git diff` 确认基线，改完立即提交。
- 用户偏好：中文简洁指令、要求截图验证、测试自动化（用户不动手）、诚实报告失败原因。
