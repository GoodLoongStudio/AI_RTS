# 交接任务：角色动画管线（九段动作已完成并接入状态机）

你是 AI_RTS 项目的美术管线助手。前一位 AI（2026-09-05）已完成：九段动画烘焙进 GLB 并在 Godot 4.7 验证通过，且**已接入步兵单位状态机**（Infantry.tscn 换 GLB 模型 + InfantryAnimationDriver 重写为 AnimationPlayer 映射，InfantryAnimDriverSmokeTest headless 18/18 全过）。剩余工作见「三、下一步」。

## 一、现状（直接用，勿重做）

- **成品资产**：`G:\AIRTS\AI_RTS\assets\models\polygon-scifi\Infantry_anim_v2.glb`（九段、4.2MB、已导入工程），管线原件在 `初选素材包\绑骨管线\4006\Infantry_anim_v2.glb`。
- **九段剪辑**（Godot 导入后名称，即状态机语义）：
  - 循环：`Idle` / `Walk` / `Run` / `Gather`（弯腰拾取）/ `Build`（跪修）
  - 单发：`Attack`（挥砍）/ `Fire`（举枪射击）/ `Hit`（受击）/ `Death`（前扑倒地，留死点）
  - 循环来自剪辑名 `-loop` 后缀（Godot glTF 导入约定：后缀转循环标志）。
- **回归测试**：`tests/automated/InfantryAnimClipsSmokeTest.gd/.tscn`——headless 跑，断言剪辑齐全/骨骼 23 根/播放推进/循环标志，带渲染时另存 27 张目检截图到 `G:\AIRTS\tmp_logs\anim_verify\`。当前 0 失败。
- **逐段自检渲染**：`初选素材包\绑骨管线\4006\动画帧v4\`（每段 a/b/c 三帧，含 ±Y 朝向标记块）。

## 二、管线（一条命令重出）

`"C:\Program Files\Blender Foundation\Blender 5.0\blender.exe" -b -P bake_godot.py`（在 `初选素材包\绑骨管线\` 下）：
约束重定向（Pass1 逐帧求值）→ 每段 a/b/c 渲染自检（`RENDER_VERIFY=True`）→ 删约束视觉烘焙 → 导出 GLB + `retargeted_baked.blend` 存档。

- 源 A = `mocap\Soldier.glb`（真人动捕，Idle/Walk/Run，mixamorig: 同名 20 骨，面朝 +Y）。
- 源 C = `mocap\ual_extract\UAL_Mannequin_skinned.glb`（Quaternius CC0，127 动作，53 骨，无前缀名）。**这是自建的**：Godot GLTFDocument 导出 skins=0，只能从 animviewer 的 pck（`unpack_pck.py` 解包 `quaternius.com/index.pck`）dump 数据后由 `build_ual_gltf.py` 重建蒙皮 glTF。想要 Mining/TreeChopping/Walk_Carry 等更多动作，需从 itch.io 的 Universal Animation Library 1/2 拿完整包。
- **烘焙四铁律（重犯必炸）**：①关键帧打段内局部帧 1..N（glTF 导出器把绝对帧号当时间）；②只烘焙映射骨（HeadTop_End/ToeBase 零长度连接骨的 matrix 分解是垃圾，Godot 无连接锁，导出会甩头 2m 撕裂蒙皮）；③跨体形源只给 Hips 拷 COPY_LOCATION、其余只拷 COPY_ROTATION（非根骨拷绝对位置撕骨链——旧 Death 压扁、Robot Punch 拉扯的根因）；④UAL 动作 30fps，输出用 ×1.25 帧映射保速。
- 想改动作清单：改 `bake_godot.py` 的 `SEG_SPEC`（源动作名/循环规格），重跑即可。

## 三、下一步（按优先级）

1. **Death 剪辑接线（需用户批准）**：`Unit._handle_unit_death()` 目前立即 `queue_free()`，播放死亡动画需改为「延迟销毁（播完 Death 约 2s）+ 关闭碰撞/选中」。只动 Unit.gd 的死亡段，先请示。
2. **批量套用 19 个其余角色**：`4006\Characters\` 下同绑骨（23 骨同名）角色，把 `bake_godot.py` 的 FBX 路径参数化后循环即可；注意各角色贴图不同，GLB 会自动内嵌。
3. **武器 prop**：步枪挂在躯干骨上（Attack/Gather 段枪位漂浮，Fire/Idle 正常），Attack/Gather 段手臂离开枪位（枪悬浮）。正解是把 prop 挂到手骨（Godot 侧用 BoneAttachment3D）或改用持枪射击类动作源。

## 四、血泪档案（勿重蹈覆辙）

- 跨 rest 重定向手写变换数学 6 种全灭（详见 memory/2026-09-04.md）——只有约束法世界空间求值 + Blender 蒙皮导入换算可行。**glTF 节点（空物体）的旋转不能直接拷给沿骨轴骨架**（rest 旋转≈单位、T-pose 编码在位置里），拷了全身折成一团。
- 朝向判定铁律：用 ±Y 标记块+渲染目检；4006 面朝 +Y，UAL Mannequin 也朝 +Y（实测一致，无需翻转）。
- Blender 5.0 API：slotted actions（`action.fcurves` 已移除，读曲线走 `act.layers[0].strips[0].channelbag(act.slots[i])`，slot 标识符=`"OB"+对象名`）；headless EEVEE 可用；`ShaderNodeBsdfPrincipled.inputs["Roughness"]`。
- 完整历史：`g:\AIRTS\.codebuddy\memory\2026-09-05.md`（本次）与 `2026-09-04.md`、`MEMORY.md`。
